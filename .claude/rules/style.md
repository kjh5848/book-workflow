# 글로벌 스타일 규칙

모든 에이전트가 준수하는 톤, 편집, 포맷 규칙.

---

## 톤

- 전체 존댓말 (-습니다/-입니다/-세요). 캐릭터 대화/독백만 구어체 유지
- 대화 표기. **역할**: "대사"
- 내면 독백. 괄호 `()` 사용 (이탤릭 금지)
- 등장인물. 역할명 사용 (**팀장**/**나**/**동료**), 조회 대상(A 사원, B 팀원). 실명 금지

## 편집

- **볼드** 양쪽 띄어쓰기 필수
- 문장 끝 콜론 금지 (`.` 사용)
- 능동태 우선 ("~되어진다" 금지)
- 영어 용어 처음 등장. 한국어(English) 형태. 이후 한국어만
- 이모지 전면 금지 (박스 스타일 포함 모든 곳에서 금지)
- 문맥 전환 시 빈 줄 삽입
- "## 이야기 파트" / "## 기술 파트" 대문 모두 제거. 전환 문장 + `---`로 대체
- 문단 구성. 같은 맥락의 문장은 빈 줄 없이 이어 쓴다. 한 줄짜리 문장이 연속으로 나열되지 않게 한다
- 숨돌리기 빈 줄. (1) 핵심 키워드/개념 도입 문장 전후 (2) 감정/반전 전환 지점 (3) 비유 마무리 직후에만 빈 줄 삽입. 단순 설명이 이어지는 구간은 문단으로 묶는다

## 금지 패턴

- 설교 ("이것이야말로...")
- 반복 강조 ("이것이 바로...이유")
- 정의 전 용어 사용
- 진부한 표현 ("두말할 필요 없이")
- 주관적 감탄 ("정말 놀라운 기능!")
- 시간 의존 표현 ("최근에 나온")
- 불필요한 감정 추임새 ("잠깐 기뻤습니다", "순간 설렜습니다" 등 서사에 기여하지 않는 한 줄짜리 감정 표현)

## 서적용 캡처

- 서적용 캡처(rich_capture)에서 ANSI 색상을 HTML 색상으로 변환하지 마라. 블랙(#222) + 볼드만 유지 → why-log.md#2026-03-15-1
- 서적용 캡처에서 불필요한 공백(min-width, padding)을 넣지 마라. 테이블이 있으면 컨테이너를 좌우로 꽉 채워라 → why-log.md#2026-03-15-1
- 서적용 캡처에서 Rich 출력의 trailing 공백을 반드시 제거하라 (COLUMNS 패딩으로 오른쪽 여백 발생) → why-log.md#2026-03-15-2
