# 1장 Docker로 시작하는 개발 환경

## 1.2 도커는 왜 필요한가?

도커(Docker)는 “프로그램을 실행하기 위한 환경을 통째로 묶어서, 어디서든 똑같이 실행되게” 해주는 도구입니다. 즉, 내 컴퓨터가 어떤 운영체제인지, 어떤 라이브러리가 깔려 있는지와 상관없이, 같은 실행 환경을 만들어서 실행할 수 있게 합니다.

---

## 1.3 실습용 Spring 서버 소개

### 실습코드

해당 실습은 깃 주소를 git clone 하고 docker compose up으로 서버를 실행하여 진행합니다. 

```python
[https://github.com/kjh5848/spring-app](https://github.com/kjh5848/spring-app)
```

해당 Spring 서버의 SpringDokerController는 health check를 위한 엔드포인트로 구성이 되어있습니다.

```python
package com.metacoding.spring_doker;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class SpringDokerController {

    @GetMapping("/")
    public String hello() {
        return "hello world!.";
    }
}
```

---

## **1.4 도커 파일 및 도커 컴포즈 살펴보기**

도커 파일 및 컴포즈는 아래 깃헙 주소를 사용합니다. 

```python
https://github.com/kjh5848/spring-docker
```

### Dockerfile

```python
# 1. Eclipse Temurin 21 기반 이미지를 사용
# - Temurin은 OpenJDK를 기반으로 한 Eclipse 재단의 공식 배포판입니다.
# - 해당 이미지는 이미 Java 21(JDK)이 설치된 리눅스 환경으로, 별도의 JDK 설치 과정이 필요 없습니다.
# - 경량화된 기본 환경을 제공하므로 빌드 속도가 빠르고 이미지 크기가 상대적으로 작습니다.
FROM eclipse-temurin:21-jdk

# 2. 필수 패키지 설치 (예: git)
# - apt-get update: 패키지 목록을 최신으로 갱신합니다.
# - apt-get install -y git: git을 설치하며, -y 옵션으로 자동 확인을 수행합니다.
RUN apt-get update && apt-get install -y git

# 3. 작업 디렉터리 설정
# - 이후 실행되는 명령어의 기준 경로를 /app으로 고정합니다.
# - 마치 “cd /app” 명령을 미리 수행한 것과 같습니다.
WORKDIR /app

# 4. 로컬 entrypoint.sh 파일을 컨테이너 내부로 복사
# - COPY <호스트경로> <컨테이너경로>
# - 이 예시에서는 현재 폴더의 entrypoint.sh를 컨테이너 /app/entrypoint.sh로 복사합니다.
COPY ./entrypoint.sh ./entrypoint.sh

# 5. entrypoint.sh에 실행 권한 추가
# - chmod +x 명령어로 해당 스크립트를 실행 가능하게 만듭니다.
RUN chmod +x ./entrypoint.sh

# 6. 컨테이너 시작 시 entrypoint.sh 자동 실행
# - ENTRYPOINT는 컨테이너가 실행될 때 반드시 호출되는 명령어를 정의합니다.
# - /bin/bash를 통해 쉘 환경에서 entrypoint.sh 스크립트를 실행합니다.
ENTRYPOINT ["/bin/bash", "./entrypoint.sh"]
```

도커 파일에서는 “이미지 안에 어떤 실행 환경을 만들지”를 정의합니다. Java 21이 설치된 기반 이미지 위에 git을 추가로 설치하고, /app 폴더를 기준 경로로 잡은 뒤, entrypoint.sh를 복사해서 실행 권한을 부여하고, 컨테이너가 시작되면 해당 스크립트를 자동으로 실행하도록 설정했습니다.

### Entrypoint.sh

```python
# 1. Git 리포지토리 주소 설정
git clone "https://github.com/kjh5848/spring-app"

# 2. 프로젝트 디렉터리로 이동
cd spring-app

# 3. gradlew 실행 권한 부여
chmod +x ./gradlew

# 4. Gradle 빌드 (테스트 생략)
./gradlew clean build

# 5. jar 파일 실행 권한 부여
chmod +x build/libs/*.jar

# 6. Spring Boot 실행 (docker 프로필 활성화)
java -jar build/libs/*.jar
```

엔트리포인트에서는 “컨테이너가 시작된 뒤 실제로 무엇을 실행할지”를 정의합니다. 먼저 실행할 Git 리포지토리 주소를 환경변수에서 읽거나 기본값으로 설정한 뒤, 해당 리포지토리를 컨테이너 내부로 클론하고 프로젝트 폴더로 이동합니다. 이후 gradlew에 실행 권한을 부여하고 Gradle로 빌드하여 jar 파일을 생성합니다.

### docker-compose.yml

```python
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spring-docker
    ports:
      - "8080:8080"
```

도커 컴포즈에서는 “그 이미지를 어떻게 실행할지”를 정의합니다. services 아래에 app이라는 이름의 컨테이너 1개를 실행하고 컨테이너 이름은 container_name으로 따로 설정했습니다.

---

## 1.5 도커 컴포즈로 Sping 서버 실행해보기

도커를 사용하기 위해 도커 앱을 실행합니다.

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image.png)

## 1) git clone

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%201.png)

원하는 폴더에 [https://github.com/kjh5848/spring-docker](https://github.com/kjh5848/spring-docker)을 클론합니다.

## 2) 도커 실행하기

```python
docker compose up
```

해당 폴더를 cursor 에디터로 열어 위 명령어를 터미널에 입력을 해줍니다.

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%202.png)

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%203.png)

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%204.png)

Dockerfile이 실행되며 eclipse-temurin:21-jdk 이미지가 다운로드 되고 docker desktop에서도 spring-docker가 정상적으로 실행이 되는 걸 확인할 수 있습니다.

---

## 1.6 실행결과 확인 및 Spring 서버 중지하기

### 1) 실행결과

이제 브라우저를 통해 실행결과를 확인해보도록 하겠습니다. 브라우저에 localhost:8080을 입력합니다.

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%205.png)

정상적으로 health check 컨트롤러가 정상적으로 실행된 것을 확인할 수 있습니다.

### 2) 서버중지

실행 중인 spring docker 컨테이너를 중지해보겠습니다.

```python
docker compose down
```

 중지에는 해당 명령어를 사용합니다.

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%206.png)

새 터미널을 열어 명령어를 실행하면 컨테이너와 네트워크가 삭제되었다는걸 확인할 수 있으며

![image.png](1%EC%9E%A5%20Docker%EB%A1%9C%20%EC%8B%9C%EC%9E%91%ED%95%98%EB%8A%94%20%EA%B0%9C%EB%B0%9C%20%ED%99%98%EA%B2%BD/image%207.png)

docker desktop에서도 실행중이었던 spring docker 컨테이너가 삭제된걸 확인할 수 있습니다.