# Ch.7: 캐시와 모니터링 (ex07)

> 한 줄 요약: 사서에게 기억력과 업무 일지를 줬다. 같은 질문엔 바로 답하고, 하루 몇 건 처리했는지 기록한다.<br>
> 핵심 개념: ResponseCache(TTL), EmbeddingCache, TokenTracker, ConnectHRAgent

---

<!-- [GEMINI PROMPT: 07_chapter-opening]
path: assets/CH07/07_chapter-opening.png
A minimalist black and white technical diagram with a strict 16:9 aspect ratio
on a solid white background. No shading, no 3D effects, only clean thin line art.
The entire assembly of icons, lines, and text is perfectly centered globally
within the 16:9 frame, leaving generous and equal white space on all sides.

Center: a minimalist line-art reception desk icon (same as CH06)
with a small notebook icon on the desk labeled '캐시'.
Left: a clock icon with circular arrow labeled 'TTL 1h'.
Right: a clipboard icon with checkmarks labeled '업무 일지'.
Below the desk: a retry arrow (circular) with '3x' label.
Style: scene-opener
-->
![운영 안정화 — 캐시와 모니터링](../assets/CH07/gemini/07_chapter-opening.png)

### 1.1 "같은 질문에 또 20초?"

CH06에서 통합 에이전트를 완성했습니다. 동료들도 만족했고요. DB 조회도 되고 문서 검색도 되고 출처까지 붙여서 답변합니다. 그런데 일주일쯤 지나자 불만이 들어오기 시작합니다.

**동료 A**: "병가 증빙 필요한지 아까도 물어봤는데, 또 물어보니까 또 20초 걸리네요."

**동료 B**: "우리 팀에서 이거 하루에 30번은 쓰는데, 비용이 얼마나 나가는 건지 모르겠어요."

같은 질문을 반복해도 매번 LLM을 호출합니다. 로컬 모델 기준 10~20초씩 걸리고, API를 쓰면 돈도 듭니다. 누가 얼마나 쓰는지 추적할 방법도 없었습니다. 문제는 또 있습니다. LLM이 가끔 에러를 냅니다. 네트워크 타임아웃이나 파싱 실패 같은 것인데, ex06에서는 에러가 나면 그대로 멈춰버렸습니다.

---

### 1.2 ResponseCache와 EmbeddingCache

CH05에서 사서에게 대화용 메모장(WindowMemory)을 줬습니다. 최근 5턴을 기억하는 메모장이었죠. 이번에는 다른 종류의 메모장을 추가합니다.

**답변 메모장 (ResponseCache)**

누군가 같은 질문을 또 했습니다. 사서가 이미 한 번 찾아본 자료라면 서가까지 다시 갈 필요가 없겠죠. 메모장에 적어둔 답을 바로 읽어주면 됩니다. 다만 메모장에는 유통기한이 있습니다. 1시간이 지나면 메모를 지워요. 사내 규정이 바뀌었을 수도 있으니까요. 이걸 **TTL(Time To Live)** 이라고 부릅니다.

**임베딩 메모장 (EmbeddingCache)**

사서가 서가에서 자료를 찾으려면 질문 문장을 벡터(숫자 배열)로 바꿔야 합니다. 이 변환에도 시간이 걸려요. 같은 문장을 여러 번 변환할 필요는 없겠죠. 한 번 계산한 벡터를 파일로 저장해두면 다음엔 바로 꺼내 씁니다.


<!-- [GEMINI PROMPT: 07_cache-concept]
path: assets/CH07/07_cache-concept.png
A minimalist black and white technical diagram with a strict 16:9 aspect ratio
on a solid white background. No shading, no 3D effects, only clean thin line art.
The entire assembly of icons, lines, and text is perfectly centered globally
within the 16:9 frame, leaving generous and equal white space on all sides.

Two side-by-side diagrams:
Left diagram labeled 'ResponseCache (인메모리)':
  A question bubble '병가 증빙?' with arrow to a box '캐시 조회',
  dashed arrow labeled 'HIT → 바로 답변' going right to answer bubble,
  solid arrow labeled 'MISS → LLM 호출' going down to LLM icon then up to answer.
  A small clock showing 'TTL: 1h'.
Right diagram labeled 'EmbeddingCache (파일)':
  A text box '연차 규정' with arrow to a box '캐시 조회',
  dashed arrow labeled 'HIT → 파일 로드' going right to vector icon [0.12, 0.87, ...],
  solid arrow labeled 'MISS → 임베딩 계산' going down to model icon then up to vector.
Style: concept-diagram
-->
![두 가지 캐시](../assets/CH07/gemini/07_cache-concept.png)
*그림 7-1: 같은 질문이 또 오면 LLM을 다시 부르지 않는다. 같은 텍스트를 또 임베딩하지 않는다.*

---

### 1.3 TokenTracker -- 토큰 추적

도서관에 이용 통계 시스템이 없다고 생각해 봅니다. 사서는 하루 종일 자료를 찾아주는데 관장이 "이번 달 이용자가 몇 명이야?"라고 물으면 대답을 못 합니다. 기록이 없으니까요.

우리 시스템도 마찬가지였습니다. 동료 B가 "비용이 얼마나 나가는 건지 모르겠어요"라고 했을 때 답할 수가 없었어요. LLM 호출 한 번에 토큰이 얼마나 들고 하루에 총 몇 건 처리하는지 아무도 몰랐습니다.

그래서 사서에게 업무 일지를 쓰게 했습니다.

- 오늘 총 몇 건 처리했는지
- 입력/출력 토큰을 얼마나 썼는지
- 평균 응답 시간은 얼마인지
- API 비용은 얼마나 나갔는지

실무에서는 **Langfuse** 나 LangSmith 같은 전문 모니터링 도구가 이 역할을 합니다. 호출별 토큰 수와 비용, 지연 시간을 자동으로 수집해서 대시보드로 보여주죠. 하지만 도구를 붙이기 전에 "무엇을 측정해야 하는지"부터 이해해야 합니다. 그래서 이 책에서는 **TokenTracker** 라는 간단한 클래스를 직접 만들어 봅니다. 매 호출마다 한 줄씩 기록이 쌓이고 나중에 "이번 달 토큰 비용이 얼마야?"라고 물으면 바로 답할 수 있어요. 도서관 이용 통계처럼 운영을 개선하려면 먼저 측정부터 해야 합니다.

---

### 1.4 Retry 로직

도서관 사서가 서가에서 자료를 꺼내려는데 하필 그 선반이 점검 중입니다. ex06의 사서는 "지금 못 찾겠어요"라고 말하고 돌아섰습니다. 이용자는 같은 질문을 처음부터 다시 해야 했죠.

LLM도 마찬가지입니다. 네트워크 오류나 타임아웃, 파싱 실패 등으로 가끔 응답을 못 합니다. ex06에서는 에러가 나면 그대로 멈췄습니다. 이제 사서에게 규칙을 하나 알려줬습니다. "한 번 실패하면 **3번까지 다시 시도해.** 시도 사이에는 2초씩 쉬고." 점검 중이던 선반도 잠깐 뒤에 다시 가면 열려 있을 수 있으니까요. 이렇게 캐시와 업무 일지, 재시도까지 운영에 필요한 안정 장치를 모두 갖추게 됐습니다.

---

### 1.5 ConnectHRAgent 표준화

ex07에서는 ex06의 에이전트를 운영 가능한 수준으로 감쌉니다.

| 기능 | 비유 | 코드 |
|------|------|------|
| 응답 캐시 | 사서의 답변 메모장 (1시간 유효) | `cache.py` |
| 임베딩 캐시 | 단어→벡터 변환 결과 저장 | `cache.py` |
| 토큰 추적 | 사서의 업무 일지 | `monitoring.py` |
| 재시도 로직 | "3번까지 다시 시도" | `agent_config.py` |

새 기능을 추가하는 게 아닙니다. 기존 에이전트를 **안정적으로 운영** 할 수 있게 감싸는 것이죠. 도구도 한 가지 바뀝니다. ex06에서 `mcp_tools.py` 하나에 몰아넣었던 도구 4개를 `tools/` 디렉토리 아래 파일 하나씩으로 분리했습니다. 도구가 늘어나도 관리가 수월해집니다.

---

이제 실습으로 캐시, 비용 추적, 에러 처리를 적용해보겠습니다.

---

### 2.1 용어 정리

| 이야기 속 표현 | 진짜 이름 | 정의 |
|---------------|----------|------|
| 답변 메모장 | **ResponseCache** | TTL(유효시간) 기반 인메모리 응답 캐시. SHA-256 해시 키로 질문을 식별하고, 만료 전까지 동일 질문에 캐시된 답변을 반환한다 |
| 임베딩 메모장 | **EmbeddingCache** | 파일 기반 임베딩 벡터 캐시. pickle로 벡터를 저장하고 프로세스 재시작 후에도 유지된다 |
| 업무 일지 | **TokenTracker** | LLM 호출별 입출력 토큰 수, 비용, 응답 시간을 누적 집계하는 추적기 |
| 캐시+재시도가 붙은 사서 | **ConnectHRAgent** | ex06의 IntegratedAgent에 캐시, 모니터링, 재시도를 통합한 운영용 에이전트 |
| 3번까지 다시 시도 | **Retry 로직** | LLM 호출 실패 시 최대 N회까지 재시도하는 안정성 패턴. 재시도 사이에 대기 시간 포함 |

### 2.2 파일 계층 구조

```
ex07/
├── run.py                    서버 실행 진입점
├── src/
│   ├── agent_config.py     [실습] ConnectHRAgent — 캐시/모니터링/재시도 통합
│   ├── cache.py            [실습] ResponseCache(TTL) + EmbeddingCache  ← 신규
│   ├── monitoring.py       [실습] TokenTracker + setup_logging  ← 신규
│   ├── llm_factory.py      [참고] LLM 인스턴스 생성 (CH05~06 패턴 동일)
│   ├── agent_helpers.py    [참고] RAG 체인 구성 + 라우팅 매핑
│   ├── router.py           [참고] 3단계 QueryRouter (CH06에서 이미 학습)
│   └── tools/
│       ├── __init__.py     [참고] 도구 패키지 초기화
│       ├── leave_balance.py  [참고] 연차 조회 도구
│       ├── sales_sum.py      [참고] 매출 합계 도구
│       ├── list_employees.py [참고] 직원 목록 도구
│       └── search_documents.py [참고] 문서 검색 도구
├── app/
│   ├── main.py             [참고] FastAPI 앱 진입점
│   ├── chat_api.py         [참고] Agent API 엔드포인트
│   └── database.py         [참고] PostgreSQL 연결
├── templates/
│   └── chat.html           [참고] 채팅 웹 UI
└── static/
    ├── css/chat.css        [참고] UI 스타일
    └── js/chat.js          [참고] 채팅 로직
```

### 2.3 실습 환경 구축

> 기본 환경(Python 3.12, Ollama, Docker)이 없다면 **부록(환경 설정)** 을 먼저 참고하세요.

```bash
cd ex07
cp .env.example .env
python3.12 -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
docker compose up -d       # PostgreSQL 실행
pip install -r requirements.txt
```

> **이전 챕터 Docker 종료**: CH06의 Docker가 실행 중이라면 `cd ex06 && docker compose down` 으로 먼저 종료하세요.

| 패키지 | 역할 |
|--------|------|
| `langchain` | 체인/에이전트 프레임워크 |
| `langchain-ollama` | Ollama LLM 연결 |
| `langchain-chroma` | ChromaDB Retriever |
| `sentence-transformers` | ko-sroberta 임베딩 |
| `psycopg2-binary` | PostgreSQL 드라이버 |
| `fastapi` | 웹 API 서버 |

### 2.4 실습 순서

ex07에서 새로 추가되는 파일은 `cache.py` 와 `monitoring.py` 두 개뿐입니다. 나머지는 ex06에서 가져와 구조만 정리한 겁니다. 핵심 코드를 아래 순서로 살펴봅니다.

1. **agent_config.py** — 캐시·모니터링·재시도를 통합한 ConnectHRAgent. ex07의 중심입니다.
2. **cache.py** — agent_config.py의 ①(캐시 조회)과 ⑤(캐시 저장)에서 사용하는 모듈입니다. ex07에서 새로 만듭니다.
3. **monitoring.py** — ④(토큰 추적)에서 사용하는 모듈입니다. 역시 ex07에서 새로 만듭니다.
4. **실행 결과 확인** — `python run.py` 로 서버를 띄우고 채팅창에서 캐시 적중을 확인합니다.

---

### 2.5 실습 1: ConnectHRAgent — 캐시/모니터링/재시도 통합 (agent_config.py)

이 파일이 ex07의 핵심입니다. ex06의 `IntegratedAgent` 에 캐시와 모니터링, 재시도를 얹은 운영용 에이전트죠. 아래 코드를 `ex07/src/agent_config.py` 에 단계별로 나누어 작성합니다.

#### 1단계: 임포트와 운영 설정 상수

```python
import os
import time

from langchain.agents import AgentExecutor, create_tool_calling_agent
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder

from .cache import response_cache
from .monitoring import token_tracker
from .tools import get_leave_balance, get_sales_sum, list_employees, search_documents
from .router import QueryRouter
from .llm_factory import build_llm
from .agent_helpers import build_rag_chain, classify_route

# --- 운영 설정 상수 ---
AGENT_MAX_ITERATIONS = 10      # Agent 최대 반복 횟수
AGENT_TIMEOUT_SECONDS = 60     # 실행 최대 대기 시간 (초)
RETRY_MAX_ATTEMPTS = 3         # 최대 재시도 횟수
RETRY_DELAY_SECONDS = 2.0      # 재시도 간격 (초)
```

ex06에는 없던 네 줄이 추가됐습니다. 타임아웃, 재시도 횟수, 재시도 간격을 상수로 빼둔 것이 운영 안정성의 시작입니다. `cache`, `monitoring`, `llm_factory`, `agent_helpers` 같은 모듈도 임포트하는데, ex06에서 한 파일에 몰아넣었던 기능을 역할별로 분리한 겁니다.

> **참고 파일**: `llm_factory.py` 는 LLM 인스턴스 생성(Ollama/OpenAI 분기), `agent_helpers.py` 는 RAG 체인 구성과 라우팅 매핑을 담당합니다. 둘 다 CH05~06에서 이미 학습한 패턴과 동일하므로 코드 설명은 생략합니다.

#### 2단계: ConnectHRAgent 클래스 — 초기화와 AgentExecutor 구성

```python
SYSTEM_PROMPT = """당신은 사내 AI 비서입니다. ..."""  # ex06과 동일

class ConnectHRAgent:
    def __init__(self):
        self.llm = build_llm()
        self._router = QueryRouter(llm=self.llm)
        self.tools = [list_employees, get_leave_balance, get_sales_sum, search_documents]
        self.rag_chain = build_rag_chain(self.llm)
        self.agent_executor = self._build_agent_executor()

    def _build_agent_executor(self):
        prompt = ChatPromptTemplate.from_messages([
            ("system", SYSTEM_PROMPT),
            MessagesPlaceholder(variable_name="chat_history", optional=True),
            ("human", "{input}"),
            MessagesPlaceholder(variable_name="agent_scratchpad"),
        ])
        agent = create_tool_calling_agent(self.llm, self.tools, prompt)
        return AgentExecutor(
            agent=agent,
            tools=self.tools,
            max_iterations=AGENT_MAX_ITERATIONS,
            max_execution_time=AGENT_TIMEOUT_SECONDS,
            handle_parsing_errors=True,
            return_intermediate_steps=True,
            verbose=True,
        )
```

`__init__` 의 구조는 ex06과 같습니다. 달라진 점은 `_build_agent_executor()` 에서 `max_iterations` 와 `max_execution_time` 을 1단계의 상수로 설정한 부분이에요. 무한 루프나 지나치게 긴 실행을 막아줍니다.

#### 3단계: _run_with_retry — 재시도 로직

```python
    def _run_with_retry(self, query, chat_history=None):
        for attempt in range(1, RETRY_MAX_ATTEMPTS + 1):
            try:
                result = self.agent_executor.invoke({
                    "input": query,
                    "chat_history": chat_history or [],
                })
                return result
            except Exception:
                if attempt < RETRY_MAX_ATTEMPTS:
                    time.sleep(RETRY_DELAY_SECONDS)
        return {"output": "3회 재시도 후 실패했습니다.", "intermediate_steps": []}
```

`for attempt in range(1, 4)` — 최대 3번 시도합니다. 실패하면 2초 쉬고 다시 돌려요. 3번 다 실패하면 에러 메시지를 반환합니다. 네트워크 지연이나 모델 과부하처럼 일시적으로 불안정한 상황에서 잘 먹히는 패턴입니다.

#### 4단계: run() — 캐시 → 라우팅 → 실행 → 추적 → 저장

```python
    def run(self, query, chat_history=None, use_cache=True):
        start_time = time.time()

        # ① 캐시 조회
        if use_cache:
            cached = response_cache.get(query)
            if cached is not None:
                cached["from_cache"] = True
                return cached

        # ② Router로 경로 결정
        route = classify_route(query, router=self._router)

        # ③ 경로별 실행
        if route == "rag" and self.rag_chain is not None:
            answer = self.rag_chain.invoke(query)
            result = {"output": answer, "route": route, "intermediate_steps": []}
        else:
            result = self._run_with_retry(query, chat_history)
            result["route"] = route

        # ④ 토큰 사용량 기록
        latency_ms = (time.time() - start_time) * 1000
        provider = os.getenv("LLM_PROVIDER", "ollama").lower()
        if provider == "openai":
            model = os.getenv("OPENAI_MODEL", "gpt-4o-mini")
        else:
            model = os.getenv("OLLAMA_MODEL", "deepseek-r1:8b")
        token_tracker.record(
            model=model,
            input_tokens=len(query.split()) * 2,   # 추정값
            output_tokens=len(result["output"].split()) * 2,
            operation="agent_run",
            latency_ms=latency_ms,
        )

        # ⑤ 캐시 저장
        if use_cache:
            response_cache.set(query, result)

        return result
```

`run()` 메서드가 ex07의 핵심 흐름입니다. ①~⑤ 다섯 단계를 순서대로 따라가면 돼요.

- **①(캐시 조회)과 ⑤(캐시 저장)** — 같은 질문이 캐시에 있으면 LLM을 호출하지 않고 바로 반환합니다. 질문이 30번 반복되면 첫 번째만 LLM을 부르고 나머지 29번은 캐시에서 가져옵니다.
- **④(토큰 추적)** — 매 호출마다 토큰 수와 응답 시간을 기록합니다. `LLM_PROVIDER` 환경 변수를 확인해서 올바른 모델명을 넘기고, TokenTracker가 모델별 단가표를 갖고 있어서 OpenAI라면 실제 비용이 계산되고 로컬 모델이면 $0입니다.

마지막으로 싱글턴 패턴을 추가합니다:

```python
_agent_instance = None

def get_agent():
    global _agent_instance
    if _agent_instance is None:
        _agent_instance = ConnectHRAgent()
    return _agent_instance
```

---

### 2.6 실습 2: ResponseCache + EmbeddingCache (cache.py)

`run()` 의 ①, ⑤ 단계에서 사용하는 캐시 모듈입니다. ex07에서 새로 만드는 파일이에요. 아래 코드를 `ex07/src/cache.py` 에 단계별로 나누어 작성합니다.

#### 1단계: 상수 정의

```python
import hashlib
import pickle
import time
from pathlib import Path

DEFAULT_RESPONSE_TTL = 3600              # 응답 캐시 TTL (초): 1시간
DEFAULT_EMBEDDING_CACHE_DIR = "./outputs/embedding_cache"
```

#### 2단계: ResponseCache — TTL 기반 인메모리 캐시

```python
class ResponseCache:
    def __init__(self, ttl=DEFAULT_RESPONSE_TTL, max_size=1000):
        self.ttl = ttl
        self.max_size = max_size
        self._store = {}
        self._hits = 0
        self._misses = 0

    def _make_key(self, query, context=""):
        raw = f"{query}::{context}"
        return hashlib.sha256(raw.encode("utf-8")).hexdigest()

    def get(self, query, context=""):
        key = self._make_key(query, context)
        entry = self._store.get(key)
        if entry is None:
            self._misses += 1
            return None
        value, expires_at = entry
        if time.time() > expires_at:
            del self._store[key]
            self._misses += 1
            return None
        self._hits += 1
        return value

    def set(self, query, value, context=""):
        key = self._make_key(query, context)
        if len(self._store) >= self.max_size:
            oldest_key = min(self._store, key=lambda k: self._store[k][1])
            del self._store[oldest_key]
        self._store[key] = (value, time.time() + self.ttl)

    def stats(self):
        total = self._hits + self._misses
        hit_rate = (self._hits / total * 100) if total > 0 else 0.0
        return {
            "total_items": len(self._store),
            "hits": self._hits,
            "misses": self._misses,
            "hit_rate_percent": round(hit_rate, 2),
        }
```

`_store` 딕셔너리에 `(값, 만료시각)` 튜플을 저장합니다. `get()` 할 때 현재 시각이 만료시각을 넘었으면 삭제하고요. 키는 `query::context` 문자열의 SHA-256 해시입니다. 1,000개가 꽉 차면 만료 시각이 가장 임박한 항목부터 지웁니다. `stats()` 에서 보이는 적중률(hit_rate)이 높을수록 LLM 호출을 아끼고 있다는 뜻입니다.

#### 3단계: EmbeddingCache — 파일 기반 임베딩 캐시

```python
class EmbeddingCache:
    def __init__(self, cache_dir=DEFAULT_EMBEDDING_CACHE_DIR):
        self.cache_dir = Path(cache_dir)
        self.cache_dir.mkdir(parents=True, exist_ok=True)
        self._hits = 0
        self._misses = 0

    def _make_cache_path(self, text):
        key = hashlib.sha256(text.encode("utf-8")).hexdigest()
        return self.cache_dir / f"{key}.pkl"

    def get(self, text):
        cache_path = self._make_cache_path(text)
        if not cache_path.exists():
            self._misses += 1
            return None
        with open(cache_path, "rb") as f:
            embedding = pickle.load(f)
        self._hits += 1
        return embedding

    def set(self, text, embedding):
        cache_path = self._make_cache_path(text)
        with open(cache_path, "wb") as f:
            pickle.dump(embedding, f)
```

ResponseCache와 구조가 같습니다. 차이점은 메모리 대신 **파일** 로 저장한다는 점이에요. 텍스트를 SHA-256 해시로 변환해서 파일명으로 쓰고, 임베딩 벡터는 pickle로 직렬화합니다. pickle은 Python 객체를 바이트 형태로 변환해서 파일에 저장하는 표준 라이브러리입니다.

#### 4단계: 싱글턴 인스턴스

```python
response_cache = ResponseCache()
embedding_cache = EmbeddingCache()
```

모듈이 임포트될 때 인스턴스가 하나 생성됩니다. 어디서든 `from .cache import response_cache` 로 같은 인스턴스를 공유하게 되죠.

> **ResponseCache vs EmbeddingCache 비교**
>
> | | ResponseCache | EmbeddingCache |
> |---|---|---|
> | 저장 위치 | 메모리 | 파일 (`.pkl`) |
> | 재시작 시 | 사라짐 | 유지됨 |
> | TTL | 있음 (기본 1시간) | 없음 (영구) |
> | 용도 | LLM 응답 재사용 | 임베딩 벡터 재계산 방지 |

---

### 2.7 실습 3: TokenTracker + JSON 로깅 (monitoring.py)

이야기 파트에서 소개한 Langfuse 같은 전문 도구의 원리를 이해하기 위해 직접 만드는 모듈입니다. `ex07/src/monitoring.py` 에 단계별로 작성해 봅니다.

#### 1단계: TokenTracker — 토큰 사용량 추적기

```python
from datetime import datetime, timezone

class TokenTracker:
    COST_PER_1K_TOKENS = {
        "gpt-4o-mini": {"input": 0.00015, "output": 0.0006},
        "deepseek-r1:8b": {"input": 0.0, "output": 0.0},  # 로컬: 무료
    }

    def __init__(self):
        self._records = []
        self._total_input_tokens = 0
        self._total_output_tokens = 0

    def record(self, model, input_tokens, output_tokens, operation="chat", latency_ms=0.0):
        cost_table = self.COST_PER_1K_TOKENS.get(model, {"input": 0.0, "output": 0.0})
        cost_usd = (
            (input_tokens / 1000 * cost_table["input"])
            + (output_tokens / 1000 * cost_table["output"])
        )
        self._records.append({
            "timestamp": datetime.now(timezone.utc).isoformat(),
            "model": model,
            "input_tokens": input_tokens,
            "output_tokens": output_tokens,
            "cost_usd": round(cost_usd, 6),
            "latency_ms": round(latency_ms, 2),
        })
        self._total_input_tokens += input_tokens
        self._total_output_tokens += output_tokens

    def summary(self):
        total_cost = sum(r["cost_usd"] for r in self._records)
        avg_latency = (
            sum(r["latency_ms"] for r in self._records) / len(self._records)
            if self._records else 0.0
        )
        return {
            "total_calls": len(self._records),
            "total_tokens": self._total_input_tokens + self._total_output_tokens,
            "total_cost_usd": round(total_cost, 6),
            "avg_latency_ms": round(avg_latency, 2),
        }
```

`COST_PER_1K_TOKENS` 에 모델별 토큰 단가를 넣어뒀습니다. 로컬 모델(deepseek-r1:8b)은 비용이 0이고 OpenAI API는 1,000토큰당 가격이 있어요. `record()` 로 호출마다 기록하고 `summary()` 로 전체 통계를 볼 수 있습니다.

#### 2단계: setup_logging — JSON 구조화 로깅

```python
import json
import logging

class JsonFormatter(logging.Formatter):
    def format(self, record):
        return json.dumps({
            "timestamp": datetime.now(timezone.utc).isoformat(),
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
        }, ensure_ascii=False)

def setup_logging(level="INFO", use_json=True):
    root_logger = logging.getLogger()
    root_logger.setLevel(getattr(logging, level.upper()))
    root_logger.handlers.clear()
    formatter = JsonFormatter() if use_json else logging.Formatter(
        "%(asctime)s | %(levelname)-8s | %(name)s | %(message)s"
    )
    handler = logging.StreamHandler()
    handler.setFormatter(formatter)
    root_logger.addHandler(handler)
```

`setup_logging(use_json=True)` 로 설정하면 로그가 JSON 형식으로 출력됩니다.

```json
{"timestamp": "2026-03-05T09:15:23Z", "level": "INFO", "logger": "src.agent_config", "message": "[ConnectHRAgent] 처리 완료 (경로: rag, 소요: 1523ms)"}
```

운영 환경에서 로그를 Elasticsearch나 CloudWatch 같은 시스템으로 보낼 때 JSON 형식이 파싱하기 쉽습니다.

#### 3단계: LangfuseMonitor (선택) + 싱글턴

```python
class LangfuseMonitor:
    def __init__(self):
        self.enabled = False
        # Langfuse 패키지가 설치되어 있고 .env에 키가 설정된 경우만 활성화

    def trace(self, name, input_data, output_data, metadata=None):
        if not self.enabled:
            return
        # Langfuse 서버로 추적 데이터 전송

token_tracker = TokenTracker()
langfuse_monitor = LangfuseMonitor()
```

이야기 파트에서 소개한 **Langfuse** 와의 연동 래퍼입니다. Langfuse 패키지가 없어도 코드는 정상 동작해요. `.env` 에 `LANGFUSE_PUBLIC_KEY` 와 `LANGFUSE_SECRET_KEY` 를 설정하면 자동으로 활성화됩니다.

---

### 2.8 [참고] 도구 모듈화

ex06에서는 `mcp_tools.py` 하나에 도구 4개가 다 들어 있었습니다. ex07에서는 `tools/` 디렉토리 아래 파일 하나에 도구 하나씩 분리했어요. 도구가 늘어나도 파일만 추가하면 됩니다.

| 파일 | 도구 | 역할 |
|------|------|------|
| `tools/leave_balance.py` | `get_leave_balance(name)` | 직원 연차 잔여 조회 |
| `tools/sales_sum.py` | `get_sales_sum(department)` | 부서별 매출 합계 조회 |
| `tools/list_employees.py` | `list_employees(department)` | 직원 목록 조회 |
| `tools/search_documents.py` | `search_documents(query)` | 문서 벡터 검색 |

---

### 2.9 실행 결과

코드 작성이 끝났습니다. 서버를 띄우고 캐시와 토큰 추적이 실제로 동작하는지 확인해 봅니다.

```bash
python run.py
```

서버가 시작되면 브라우저에서 `http://localhost:8000` 으로 접속합니다.

#### 1. 첫 질문 처리 — 에이전트가 도구를 호출한다

채팅창에서 "김민준 연차 잔여일수 알려줘"를 입력합니다. 터미널 로그를 보면 에이전트가 어떤 순서로 일하는지 한눈에 볼 수 있어요.

![첫 질문 처리 로그](../assets/CH07/terminal/07_log-first-query.png)
*그림 7-2: 첫 질문 처리 흐름. 에이전트가 도구를 호출하고, 토큰을 기록하고, 캐시에 저장한다.*

로그를 위에서 아래로 읽어봅니다.

1. **질문 수신** — ConnectHRAgent가 질문을 받습니다.
2. **AgentExecutor 체인 시작** — LLM이 질문을 분석하고, `get_leave_balance` 도구를 선택합니다.
3. **도구 호출** — 김민준의 연차 데이터를 DB에서 조회합니다. 잔여 8일이 돌아옵니다.
4. **LLM 응답 생성** — 조회 결과를 바탕으로 자연어 답변을 만듭니다.
5. **TokenTracker 기록** — 입력 8토큰, 출력 12토큰, 소요 15,933ms를 기록합니다.
6. **ResponseCache 저장** — 답변을 캐시에 저장합니다. 3,600초(1시간) 후 만료됩니다.

전체 처리에 약 16초가 걸렸습니다. LLM 호출 두 번(질문 분석 + 답변 생성)이 시간을 거의 다 잡아먹었네요.

#### 2. 캐시 적중 — 같은 질문엔 바로 답한다

같은 질문을 여러 번 더 입력해 봅니다. 터미널 로그가 확연히 달라집니다.

![캐시 적중 로그](../assets/CH07/terminal/07_log-cache-hit.png)
*그림 7-3: 같은 질문을 반복하면 캐시에서 즉시 반환한다. LLM을 호출하지 않는다.*

첫 질문과 비교하면 차이가 확실합니다.

| | 첫 질문 | 캐시 적중 |
|---|---|---|
| LLM 호출 | 2회 (분석 + 생성) | 0회 |
| DB 조회 | 1회 | 0회 |
| 소요 시간 | ~16초 | 즉시 |
| 로그 | AgentExecutor 체인 전체 | `[ResponseCache] 적중` 한 줄 |

`잔여 TTL: 3386초` — 캐시가 저장된 지 약 3분이 지났다는 뜻입니다(3600 - 3386 = 214초). TTL이 0이 되면 캐시가 만료되고 다음에 같은 질문이 들어오면 다시 LLM을 호출합니다.

#### 3. 상태 확인 대시보드

사이드바의 **"상태 확인"** 을 클릭하면 캐시 적중률과 토큰 사용량을 한눈에 볼 수 있습니다.

![상태 확인 대시보드](../assets/CH07/terminal/07_stats-dashboard.png)
*그림 7-4: 상태 확인 페이지에서 캐시 적중률 83.33%와 토큰 사용량을 확인한다.*

세 가지 카드가 표시됩니다.

- **서버 상태** — 서버가 정상(ok)인지 버전은 무엇인지 보여줍니다.
- **응답 캐시 (ResponseCache)** — 적중률 83.33%는 6번 질문 중 5번을 캐시에서 답했다는 뜻입니다. 미스 1건은 첫 질문이에요. 캐시가 비어 있었으니 당연히 미스입니다.
- **토큰 사용량 (TokenTracker)** — 입력/출력 토큰 수와 추정 비용을 보여줍니다. 로컬 모델(Ollama)은 비용이 $0이고, OpenAI API를 쓰면 모델별 단가표에 따라 실제 비용이 계산돼요.

이 대시보드는 `http://localhost:8000/api/stats` API의 JSON 데이터를 웹 UI로 시각화한 겁니다.

---

### 2.10 더 알아보기

**TTL을 얼마로 잡아야 하나요?** 사내 문서가 자주 바뀌면 짧게(30분), 거의 안 바뀌면 길게(24시간) 잡으면 됩니다. 기본값 1시간은 대부분의 사내 환경에서 적당합니다. `.env`에서 `CACHE_TTL=3600`으로 조정할 수 있어요.

**ResponseCache가 메모리 기반이면 서버 재시작하면 다 날아가지 않나요?** 맞습니다. 운영 환경에서는 Redis 같은 외부 캐시를 쓰는 게 일반적이에요. 이 책에서는 개념 이해를 위해 인메모리 구현을 사용합니다. Redis로 바꾸려면 `get()`과 `set()`만 Redis 클라이언트로 교체하면 됩니다.

**토큰 수가 추정값인 이유** Ollama는 응답에 토큰 사용량을 포함하지 않습니다. `단어 수 × 2`는 한국어 기준 대략적인 추정이에요. OpenAI API를 쓰면 정확한 토큰 수가 응답에 포함됩니다.

---

### 2.11 이것만은 기억하세요

- **같은 질문엔 캐시로 답합니다.** ResponseCache가 TTL 동안 답변을 기억해서 LLM 재호출을 막아줍니다.
- **운영은 기록에서 시작합니다.** TokenTracker로 토큰 사용량과 비용을 추적해야 운영 비용을 관리할 수 있어요.
- 다음 챕터부터는 이 에이전트의 **검색 품질** 을 올립니다. "엉뚱한 문서를 가져온다"는 문제를 청킹 최적화와 리랭킹, 하이브리드 검색으로 해결합니다.
