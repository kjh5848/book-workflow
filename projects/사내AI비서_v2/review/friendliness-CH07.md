# CH07 친절도 점검 보고서

> 11질문 패널 점검. CH07은 운영 안정화(ResponseCache + EmbeddingCache + TokenTracker + Retry) 챕터로, 챕터 6의 IntegratedAgent를 그대로 두고 바깥에 운영 래퍼 3종을 씌우는 구조. 강화된 Q4 기준(메타 내레이션·구어체 잔재·`~봅니다`·본문 `~죠`·`~봤어요` 등) + Q11(CH01·CH02·CH03·CH04·CH05·CH06 회수) 동시 점검.
>
> **Q6/Q7 점검 범위**: CLAUDE.md 독자 상수 "배경지식 있음, 이 책의 주제만 모름" 적용. 일반 웹/인프라 용어(FastAPI·uvicorn·Docker·Ollama·PostgreSQL·ChromaDB 패키지·Python `pickle`·`SHA-256`·`hashlib` 같은 표준 라이브러리·환경변수·logger 등)는 정의·표 등재 면제. **도메인 용어**(ResponseCache·EmbeddingCache·TokenTracker·ConnectHRAgent·TTL·Retry·Langfuse·LangSmith·observability·`compute_fn` 같은 RAG 운영 도메인)만 점검.

## 한눈에 보기

| # | 영역 | 판정 | 핵심 위반 |
|---|------|------|----------|
| 1 | 서사 연속성 (챕터 안) | ✅ PASS | — |
| 2 | 캐릭터 등장 일관성 | ✅ PASS | 7.1·7.2·7.3·7.4·7.5에 캐릭터 분산 등장 (도입·중간·끝 모두 충족) |
| 3 | 비유 회수 | ✅ PASS | 메모장·서가·사서·업무 일지·매뉴얼 비유 16회 회수 + `:::remember` 항목에 비유 보존 |
| 4 | 본문 톤 (종결어미·구어체·메타) | ⚠ CONDITIONAL_PASS | **본문 `~죠` 종결 1건**(line 133 "감싸는 작업이죠"). CH05·CH06 FAIL 패턴은 크게 줄었으나 1건 잔존 |
| 5 | 캐릭터 대사 톤 | ✅ PASS | 동료 3회·팀장 4회·오픈이 4회 모두 존댓말. 반말·`~고요`·`~구요` 0건 |
| 6 | 용어 첫 등장 처리 | ✅ PASS | TTL·ResponseCache·EmbeddingCache·TokenTracker·ConnectHRAgent·`compute_fn`·Retry 모두 비유(메모장·업무 일지·운영 래퍼) → 한국어(English)·정식 정의 2단계 충족 |
| 7 | 용어 정리 표 완결성 | ✅ PASS | 5개 표 항목(ResponseCache·EmbeddingCache·TokenTracker·Retry 로직·ConnectHRAgent) 모두 본문에 등장하고 비유→진짜용어→정식정의 3열 일치 |
| 8 | 실습 코드블록 앞 안내 | ❌ FAIL | **표준 안내 누락 4건**(L319·L351·L423·L671). `[실습 1]` 헤더·번호 일관성은 OK이지만 "파일 경로를 열고 TODO의 `pass`를 지우고 아래 코드를 작성합니다" 표준 형식이 코드블록 직전에 명문으로 없음 |
| 9 | 코드 후 설명 충분성 | ✅ PASS | 코드블록 7개 모두 직후 1~3문단 풀이 + `:::term-box` 1개(L417·`compute_fn`) + 보조 함수 안내(L421·453·570) + "왜 이 구조" 설명 |
| 10 | 섹션 전환 부드러움 | ⚠ CONDITIONAL_PASS | 7.4 → 7.5 전환(line 729→730)이 캐릭터·내면독백 다리 없이 코드블록 직후 곧장 서버 실행으로 점프. 7.5 → 7.6도 닫는 대사 다음 곧장 FAQ 섹션으로 점프 |
| 11 | 이전 챕터 맥락 회수 | ✅ PASS | CH06 IntegratedAgent·AgentExecutor·ReAct·MCP Tools·사후 분류·Router 회수(L105·133·184·582·591·690·728)·CH05 사서·메모장(WindowMemory) 회수(L198)·CH06 ex06 서버 핸드오프(L732) 모두 명시. 그림 6-2/6-7 → 그림 7-10 아키텍처 발전 흐름 일관 |

**종합**: PASS 8 / CONDITIONAL_PASS 2 / FAIL 1 (총 11개 중)

## 우선 보강 항목

### FAIL (즉시 수정 필요)

**Q8. 실습 코드블록 앞 안내 — 표준 형식 4건 누락 (FAIL)**

`code.md` "[실습] 코드블록 앞 필수 안내" 룰 위반. 룰 본문:

> **`[실습]` 코드블록 바로 위에 반드시 다음 두 가지를 포함한다.**
> 1. 파일 경로: `` `tuning/step1_query_rewrite/hyde.py` `` 형태로 독자가 열어야 할 파일 안내
> 2. 파일 열기 + TODO 안내: "를 열고 TODO의 `pass`를 지우고 아래 코드를 작성합니다"

CH07에는 `[실습]` 코드블록이 7개(L299·319·351·423·482·507·671). 이 중 **4개**가 직전 1~3줄 안에 표준 안내 누락:

| 코드블록 | 위치 | 직전 안내 (현재) | 위반 |
|---------|------|----------------|------|
| `[실습 1] cache.py. ResponseCache.get` | L319 | "이제 조회 메서드 `get()`입니다. **세 가지 경로**로 갈라집니다. 키 없음 → 만료됨 → 적중. 어느 쪽이든 카운터 갱신과 로깅을 잊지 않아야 합니다." | **파일 경로 + TODO `pass` 안내 모두 없음** |
| `[실습 1] cache.py. ResponseCache.set` | L351 | "저장할 때는 **용량 관리**가 함께 들어갑니다. 메모장 페이지가 `max_size`(기본 1000)를 넘으면 **만료 시각이 가장 이른 항목**부터 한 건 지우고 새 항목을 넣습니다." | 파일 경로·TODO 안내 모두 없음 |
| `[실습 1] cache.py. EmbeddingCache` | L423 | "파일 I/O의 귀찮은 부분(해시 파일명, `pickle` 직렬화, 히트·미스 카운팅)은 `_cache_utils.py`의 `embedding_get` · `embedding_set`이 맡습니다. 우리는 **이 두 헬퍼를 어떤 순서로 호출할지**만 결정합니다." | 파일 경로(`ex07/src/cache.py`)·TODO 안내 없음. `_cache_utils.py` 보조파일만 명시되어 독자가 어느 파일을 열어야 할지 헷갈림 |
| `[실습 3] agent_config.py. ConnectHRAgent.run` | L671 | "`run_with_retry`는 최대 3회까지 …. 매 실패에 `logger.warning`으로 …. 이제 `ConnectHRAgent.run()` 차례입니다." | 코드블록 직전 표준 안내 없음. (실습 3 도입부 L633에 한 번 등장하지만 그사이에 `[참고] _agent_utils.py` 코드블록이 끼어 들어가 직전 1~3줄 룰을 충족하지 못함) |

**충족된 코드블록**:
- L299 `[실습 1] cache.py. ResponseCache — 초기화` — L295~297에 "TODO의 `pass`를 지우고 아래 코드를 채워 보겠습니다" + L297 클래스 골격 설명. 단, 파일 경로 직접 언급은 L295 `_cache_utils.py`(보조)뿐. 본 파일(`cache.py`)은 직전 H3(L285) `### 실습 1. cache.py`로만 등장. 문구도 룰 표준 "작성합니다"가 아니라 "채워 보겠습니다" 변형. 경계선 PASS.
- L482 `[실습 2] TokenTracker — 골격` — L478에 "우리가 채울 TODO는 `record()` 한 곳이며, `pass`를 지우고 아래 코드를 작성합니다" + L480 "먼저 **클래스 골격**입니다" 명문 + 파일 경로(L478 `ex07/src/monitoring.py`). PASS.
- L507 `[실습 2] TokenTracker.record` — L505 "다음은 **TODO가 있는 `record()` 메서드**입니다" + 처리 단계 안내. 파일 경로는 L478에 한 번. 경계선 PASS (실습 2 안에서 두 번째 코드블록이라 직전 한 번이면 충분).

**원인 추정**: 실습 1·2·3 각 도입부(`### 실습 N. 파일명`)에서 파일 경로를 한 번 명시한 뒤, 같은 파일 내부의 분할 코드블록에서는 직전 안내를 생략하는 패턴. 그러나 `code.md` 룰은 **`[실습 N]` 코드블록 단위**로 직전 1~3줄 안에 명시 의무. 분할 코드블록도 룰 적용 대상.

**수정 권장**:

L319 직전 (현재 L317~318):
```
> 이제 조회 메서드 `get()`입니다. **세 가지 경로**로 갈라집니다. 키 없음 → 만료됨 → 적중.
```
→
```
> `ex07/src/cache.py`의 `ResponseCache.get()` TODO의 `pass`를 지우고 아래 코드를 작성합니다.
> **세 가지 경로**로 갈라집니다. 키 없음 → 만료됨 → 적중.
```

L351 직전 (현재 L349):
```
> 저장할 때는 **용량 관리**가 함께 들어갑니다. 메모장 페이지가 `max_size`(기본 1000)를 넘으면 …
```
→
```
> `ex07/src/cache.py`의 `ResponseCache.set()` TODO의 `pass`를 지우고 아래 코드를 작성합니다.
> 메모장 페이지가 `max_size`(기본 1000)를 넘으면 …
```

L423 직전 (현재 L421):
```
> 파일 I/O의 귀찮은 부분 …은 `_cache_utils.py`의 `embedding_get` · `embedding_set`이 맡습니다.
```
→
```
> `ex07/src/cache.py`의 `EmbeddingCache.get_or_compute()` TODO의 `pass`를 지우고 아래 코드를 작성합니다.
> 파일 I/O의 귀찮은 부분 …은 `_cache_utils.py`의 `embedding_get` · `embedding_set`이 맡습니다.
```

L671 직전 (현재 L669):
```
> `run_with_retry`는 최대 3회까지 …. 이제 `ConnectHRAgent.run()` 차례입니다.
```
→
```
> 이제 `ex07/src/agent_config.py`의 `ConnectHRAgent.run()` TODO의 `pass`를 지우고 아래 코드를 작성합니다.
> `run_with_retry`는 최대 3회까지 …
```

---

### CONDITIONAL_PASS (수정 권장)

**Q4. 본문 톤 — `~죠` 본문 종결 1건 (CONDITIONAL_PASS)**

CH05·CH06 FAIL 패턴(`~죠` 본문 종결 5건)이 CH07에서 **1건으로 크게 감소**. 다만 1건 잔존:

| 라인 | 현재 (위반) | 권장 (PASS) | 위치 |
|------|-----------|------------|------|
| 133 | "엔진을 재설계하는 게 아니라 바깥을 감싸는 **작업이죠**." | "엔진을 재설계하는 게 아니라 바깥을 감싸는 **작업입니다**." | 7.1 절 마무리 — ConnectHRAgent 도입 문장 |

**검출되지 않은 항목 (PASS 영역)**:
- `~봅시다`·`~봅니다` 0건 ✅
- `~고요·~구요` 0건 ✅
- `~봤어요·~었어요` 본문 0건 ✅ (대사 안 1건 line 772 "안 돌린 거잖아요" 동료 대사로 OK)
- AI 선호어(드디어·마침내·비로소·진정한·결국 이 모든 것) 본문 0건 ✅
- 메타 내레이션("그 사람"·"판을 뒤집었던") 0건 ✅
- 정의되지 않은 비유 도약 0건 ✅
- `~인 거죠·~잖아요·~이거든요` 본문(서술) 0건 ✅ (대사 내부만 등장 — 화자 캐릭터성 OK)

**참고 — em dash(`—`) 사용**: 본문에서 em dash `—`가 H2(line 455 `## 7.3 TokenTracker — 업무 일지를 쓴다`, line 572 `## 7.4 Retry와 ConnectHRAgent — 운영용 래퍼`)와 본문 목차(line 94~97 `1. **7.2 캐시** — ResponseCache·EmbeddingCache 이론`)에 다수 사용. `style.md` 편집 규칙: "대시 `--`·`—`(em dash)·`–`(en dash) 금지. 섹션 제목(H2/H3)에서 부연이 필요하면 단일 하이픈 `-` 하나만 사용". **이 항목은 Q4 친절도가 아닌 `style.md` 편집 룰 위반**이므로 친절도 점검 범위 밖이지만 보고에 동반 기록 — **별도 일괄 교정 권장 (em dash 18개 본문 전수, H2 2건과 본문 목차 4건 우선)**.

---

**Q10. 섹션 전환 부드러움 — 7.4 → 7.5 / 7.5 → 7.6 다리 부재 (CONDITIONAL_PASS)**

`storytelling.md` "장면 전환" 룰: "이전 장면의 마지막 이미지를 다음 장면 첫 줄에 겹치거나 연결한다." `chapter-format.md` 12단계: "스토리 전환 — 캐릭터 대사·내면독백 → 다음 실험의 문제 제기".

| 전환 지점 | 현재 (위반/약함) | 위치 |
|---------|---------------|------|
| 7.4 끝 → 7.5 시작 | L728 코드 후 설명 마지막 문장("…임시 계산입니다.") 직후 곧장 L730 `## 7.5` 헤더 + L732 "챕터 6의 ex06 서버가 켜져 있다면 Ctrl+C로 먼저 종료합니다" 운영 안내 | L729→L730 다리 0줄. 캐릭터·내면독백 부재 |
| 7.5 끝 → 7.6 시작 | L774 동료/오픈이 대사 닫기 직후 곧장 L776 `## 7.6 더 알아보기` FAQ 섹션 | L775 다리 0줄. 챕터 안 마지막 캐릭터 대사가 끝났는데 곧장 FAQ로 점프하여 서사 종료 직후 사실 정보로 톤이 흔들림 |

**대조 — 잘 된 전환**:
- 7.1 → 7.2 (L186→L188→L190): 그림 7-2 캡션 "운영 래퍼 3층" 닫기 → "메모장 이야기는 팀장 자리로도 이어졌습니다. 오픈이가 화이트보드 앞에 서서 마커 뚜껑을 딸깍 열었습니다." 비유 다리 + 감각 묘사 + 캐릭터 행동 ✅
- 7.2 → 7.3 (L453→L455→L457): "지루한 I/O는 전부 헬퍼가 맡고" 닫기 → L457 "화이트보드의 두 번째 네모가 비어 있었습니다. 동료가 던졌던 두 번째 불만이 떠올랐습니다." 화이트보드 비유 회수 + 내면독백 ✅
- 7.3 → 7.4 (L570→L572→L574): "기록을 쌓는 부분만 구현합니다" 닫기 → L574 "세 번째 네모에는 슬랙의 '에러가 났어요' 메시지가 떠올랐습니다." 화이트보드 + 슬랙 비유 다리 ✅

**수정 권장**:
- 7.4 → 7.5 사이에 캐릭터 대사 또는 내면독백 1줄 삽입. 예: 오픈이가 코드를 다 짠 뒤 "이제 돌려 보겠습니다" 같은 행동 다리.
- 7.5 → 7.6 사이에 "오픈이는 의자에서 일어나며 한 번 더 떠오른 의문 몇 가지를 정리해 봤습니다" 같은 FAQ 도입 다리. FAQ 자체가 운영 환경 후속 의문이라는 맥락을 캐릭터에 묶어 주면 톤 일관.

---

## 상세

### 질문 1. 서사 연속성 (PASS)

**판정**: PASS

**근거**:
- 7.1 도입 동료 두 불만(20초 대기 L107·사용량 파악 안 됨 L115) → 7.2(메모장으로 첫 불만 해결) → 7.3(업무 일지로 두 번째 불만 해결) → 7.4(슬랙 "에러" 메시지로 세 번째 불만 해결) → 7.5에서 동료가 직접 "캐시 적중률 72%" 검증 대사로 회수(L772). 원형 서사 명확.
- 화이트보드 세 네모 메타포(L190 "마커 뚜껑을 딸깍" → L457 "화이트보드의 두 번째 네모가 비어 있었습니다" → L574 "세 번째 네모에는 슬랙의 '에러가 났어요' 메시지가 떠올랐습니다") — 세 절 도입을 같은 화이트보드 장면으로 묶어 서사 흐름 견고.
- 7.5 마무리 동료 검증 대사(L772) → 오픈이 응답(L774) → 7.6 FAQ로 자연 종료. 끝낸 이야기를 그대로 두고 새 이야기를 시작한 절 0건.

### 질문 2. 캐릭터 등장 일관성 (PASS)

**판정**: PASS

**근거**:
- 7.1: 동료(L107·115) + 팀장(L123·127) + 오픈이(L125·내면독백 L111·119·131) — 셋 모두 등장
- 7.2: 팀장(L192·196) + 오픈이(L194)
- 7.3: 오픈이(L459) + 팀장(L461)
- 7.4: 팀장(L576) + 오픈이(L578)
- 7.5: 오픈이 내면독백(L768) + 동료(L772) + 오픈이(L774) — 원형 서사 닫기
- **5절 모두 캐릭터 등장**. 5~7페이지 이상 부재 구간 0건. 한 챕터에 인물 셋 모두 다회 등장 충족 (오픈이 4회, 팀장 4회, 동료 3회).

### 질문 3. 비유 회수 (PASS)

**판정**: PASS

**근거**:
- 도입부 비유 6종: 메모장(L131·190·192·196·198·202·219·225·231·239·241·386·390·391) / 사서(L103·123·461·576·588) / 서가(L123·202·219·231·239) / 업무 일지(L103·455·463·588) / 매뉴얼(L576) / 화이트보드 + 슬랙(L190·457·574)
- **메모장 비유는 16회 회수** — 7.2 도입(L131 내면독백) → 비유 클래스 다이어그램(L239 캡션 "유통기한이 있는 메모장") → 코드블록 주석(L307 "메모장 본체") → 코드 후 설명(L315·349 "메모장은 항상 max_size 아래") → 비교 박스(L390·391) → 절 닫기(L386 "여기까지가 답변 메모장입니다")
- **`:::remember` 박스에 비유 보존 0건이 아닌 PASS** — "같은 질문은 같은 답이 빠르게"(L898), "측정이 개선의 시작입니다"(L899), "실패는 기본입니다"(L900) — 비유 그대로는 아니지만 메타포의 핵심 의미("빠르게/측정/실패")가 한 줄 요약으로 흡수
- **용어 정리 표(L887~893)**가 본문 비유→진짜 용어→정식 정의 3열 일치 — "답변 메모장"·"임베딩 메모장"·"업무 일지"·"3번까지 다시 시도"·"운영 래퍼" 모두 등재

### 질문 4. 본문 톤 (CONDITIONAL_PASS)

**판정**: CONDITIONAL_PASS — 위 "우선 보강 항목" 참조

**근거 (위반)**:
- L133 본문 "감싸는 **작업이죠**" — `~죠` 종결 본문 1건. 같은 문단 안에 `~합니다`·`~부르겠습니다`·`~그대로입니다` 4종이 정상 사용되는데 한 문장만 일탈

**근거 (PASS 영역)**:
- 종결어미 5종(`~합니다·~입니다·~세요·~보겠습니다·~습니다`) 외 일탈 0건 (위 1건 제외)
- `~봅시다`·`~봅니다`·`~고요·~구요` 0건 ✅
- AI 선호어 0건 ✅
- 메타 내레이션 0건 ✅
- 정의 누락 비유 도약 0건 ✅

### 질문 5. 캐릭터 대사 톤 (PASS)

**판정**: PASS

**근거**:
- 14개 대사 모두 존댓말. 반말 0건.
- 동료 3개 대사: "기다려야 해요?"(L107) · "파악이 안 돼요"(L115) · "안 돌린 거잖아요"(L772) — 모두 `~요·~잖아요`로 캐주얼 존댓말
- 팀장 4개 대사: "어떻겠어요?"(L123) · "필요해요?"(L127) · "할 건데요?"(L192) · "독이 돼요"(L196) · "쓰게 해요"(L461) · "쥐여 줘요"(L576) — 모두 `~요·~죠` 캐주얼 존댓말. 인용문 안 반말("한 번 실패하면 3번까지 다시 시도해. 시도 사이에 2초씩 쉬고." L576)은 매뉴얼 인용으로 룰 허용
- 오픈이 4개 대사: "비효율적이죠"(L125) · "정도요"(L194) · "안 되고 있어요"(L459) · "흡수되겠네요"(L578) · "줄어들 거예요"(L774) — 모두 `~요·~죠·~네요` 일관 존댓말
- `~고요·~구요` 0건 ✅. CH06 학습 정착.

### 질문 6. 용어 첫 등장 처리 (PASS)

**판정**: PASS

**근거 (도메인 용어 한정 점검)**:
- **TTL**(Time To Live, 유효 시간) — `:::goal`(L6 `ResponseCache(TTL)`) → 본문 첫 등장 L202 "**유통기한(TTL)** 이 있습니다" — 비유(유통기한) + 영문 약어 형태. 후속 사용 자연스럽게 정착. ✅
- **ResponseCache** — L202 "답변 메모장" 비유로 도입 → L283 "유통기한이 있는 메모장" 캡션 → L300~313 클래스 정의 → L889 표 "TTL 기반 인메모리 응답 캐시. SHA-256 해시 키로 질문을 식별". 비유→정의 2단계 충족. ✅
- **EmbeddingCache** — L243 "텍스트별 전용 서랍" 비유 → L283 캡션 → L424 클래스 정의 → L890 표 "파일 기반 임베딩 벡터 캐시. pickle로 저장·유지". ✅
- **TokenTracker** — L463 "사서한테 업무 일지를 쓰게 해요" 팀장 대사 → L470 정의 + Langfuse·LangSmith 대안 도구 언급 → L473 `:::term-box` "TokenTracker 왜 직접 만드나?" → L891 표. ✅
- **ConnectHRAgent** — L133 "이름만 새 옷일 뿐 속은 챕터 6 그대로" 비유 → L582 "챕터 6의 IntegratedAgent에 캐시·모니터링·재시도를 얹은 버전" 정의 → L893 표. ✅
- **Retry** — L574 "재시도 매뉴얼" 비유 → L580 "잠깐 뒤에 다시 시도하면 대부분 풀립니다 …선반도 잠깐 뒤엔 열려 있을 수 있기 때문" 비유 + L9 `:::goal` 영문 명시 → L892 표 "Retry 로직". ✅
- **`compute_fn`** — L415 본문에서 첫 등장 + L417 `:::term-box`로 "캐시 미스일 때 값을 계산할 함수" 정식 정의. ✅ 이상적 비유→정의 패턴.
- **observability·Langfuse·LangSmith** — L470 "전문 모니터링 도구"라는 한 줄 정의로 일관 처리. 도구 이름이라 정의 면제 룰 적용. ✅

**점검 면제**: 일반 인프라 용어(SHA-256·`pickle`·Redis·`logger`·`Path`·`hashlib` 등). CLAUDE.md 독자 상수 적용.

### 질문 7. 용어 정리 표 완결성 (PASS)

**판정**: PASS

**근거**:
- 표 5개 항목(L885~893): ResponseCache · EmbeddingCache · TokenTracker · Retry 로직 · ConnectHRAgent — 모두 본문에 본 용어로 등장 + 비유로도 회수 + 표 정식 정의 일치
- 본문에 있고 표에 빠진 도메인 용어: **0건**
  - `compute_fn`은 `:::term-box`로 본문에 직접 정의되어 표에 안 들어가도 OK
  - Langfuse·LangSmith는 대안 도구 이름이라 표 등재 면제
  - TTL은 `유통기한(TTL)`로 ResponseCache 정의에 흡수되어 표 별도 항목 불필요
- 표에만 있고 본문에 안 쓰인 용어: **0건**
- **3열 일치(비유→진짜 용어→정식 정의)**: ResponseCache 행 = "답변 메모장" / **ResponseCache** / "TTL 기반 인메모리 응답 캐시. SHA-256 해시 키로 질문을 식별" — 본문 L189·L200·L300의 사용과 정확히 매핑. 다른 4개 행 동일하게 검증.

### 질문 8. 실습 코드블록 앞 안내 (FAIL)

**판정**: FAIL — 위 "우선 보강 항목" 참조

**근거 (위반 4건)**: L319·L351·L423·L671. 직전 1~3줄 안에 (1) 파일 경로 백틱 + (2) "TODO의 `pass`를 지우고 아래 코드를 작성합니다" 표준 안내 모두 또는 일부 누락.

**근거 (PASS 영역)**:
- `[실습 N]` 헤더 번호: 실습 1·2·3 순서대로 시작. CH06 `[실습 1]` 누락 패턴 재발 0건 ✅
- 실습 순서 표(L92~97 4단계)와 헤더 번호 일관성: 실습 1=cache.py, 실습 2=monitoring.py, 실습 3=agent_config.py — 표(L94~96)와 헤더(L285·476·631)에서 정확히 매칭 ✅
- 실습 1 도입(L295)·실습 2 도입(L478)·실습 3 도입(L633)에 한 번씩은 표준 형식 부분 충족 ✅

### 질문 9. 코드 후 설명 충분성 (PASS)

**판정**: PASS

**근거**:
- 코드블록 7개 모두 직후 1~3문단 핵심 동작 풀이:
  - L315 ResponseCache 초기화 → "self._store가 메모장 본체. 만료 시각을 같이 저장해 두는 것이 TTL 구현의 핵심"
  - L347 ResponseCache.get → "2·3·4 분기가 모두 카운터를 갱신"
  - L370 ResponseCache.set → "min(self._store, key=lambda k: self._store[k][1])이 가장 먼저 만료될 항목"
  - L453 EmbeddingCache → "make_response_key, embedding_get, embedding_set는 _cache_utils.py의 보조 함수"
  - L503 TokenTracker 골격 → "로컬 모델은 비용이 0이라 단가 표에 0.0으로 못 박혀"
  - L556 TokenTracker.record → "3단계의 누적 갱신 / 4단계의 Rich Table 출력 / Table·console은 파일 상단에서 import"
  - L728 ConnectHRAgent.run → "1단계와 5단계가 실습 1에서 만든 response_cache와 맞물리고, 4단계가 실습 2의 token_tracker에 기록을 쌓습니다" — **실습 간 부품 조립 명시** (`code.md` "실습 간 연결" 룰 충족)
- `:::term-box` 1개(L417 `compute_fn`) — "왜 이 구조" 명시 ✅
- 보조 함수 안내: L295·421·453(`_cache_utils.py` 헬퍼 3개)·L570(`_monitoring_utils.py` 헬퍼 3개)·L633(`_agent_utils.py` 2개) — 외부 헬퍼 출처 명시 ✅
- 매개변수·외부 주입 출처: L297 "용량 한도(max_size), 유통기한(ttl), 히트·미스 카운터" + L82~88 `.env` 표(`CACHE_TTL`·`CACHE_MAX_SIZE`·`EMBEDDING_CACHE_DIR`)로 외부 주입 출처 명시 ✅
- 실습 1→2→3 부품 조립 흐름: L478·L728에서 두 번 명시 ✅

### 질문 10. 섹션 전환 부드러움 (CONDITIONAL_PASS)

**판정**: CONDITIONAL_PASS — 위 "우선 보강 항목" 참조

**근거 (PASS 영역)**:
- 7.1 → 7.2(L186→L188→L190): 화이트보드 비유 다리 + 캐릭터 행동 ✅
- 7.2 → 7.3(L453→L455→L457): 화이트보드 두 번째 네모 회수 + 내면독백 ✅
- 7.3 → 7.4(L570→L572→L574): 화이트보드 세 번째 네모 + 슬랙 비유 ✅

**근거 (위반·약함)**:
- 7.4 → 7.5(L728→L730): 코드 후 설명 직후 곧장 헤더 + 운영 안내. 캐릭터·내면독백 다리 0줄
- 7.5 → 7.6(L774→L776): 동료 검증 대사 직후 곧장 FAQ 헤더. 문맥 전환 시 빈 줄 + 다리 문장 부재

### 질문 11. 이전 챕터 맥락 회수 (PASS)

**판정**: PASS

**근거**:

| 회수 대상 | 본문 라인 | 회수 방식 |
|----------|---------|---------|
| **CH06 IntegratedAgent** | L105·133·140·142·184·582·617·690·728 | "챕터 6에서 통합 에이전트를 완성한 뒤" + 그림 7-2/7-6에서 "챕터 6 그대로" 뱃지로 시각 회수 + 본문 "엔진을 재설계하는 게 아니라 바깥을 감싸는 작업" 명시 |
| **CH06 AgentExecutor + ReAct** | L144·184·620·690 | 그림 7-2/7-6에서 코어 박스 + "AgentExecutor + ReAct 루프" 텍스트 + 본문 "ex06의 IntegratedAgent 엔진 그대로" |
| **CH06 MCP Tools (4개)** | L145 그림 + L591 본문 | "챕터 6에서 mcp_tools.py 하나에 몰아넣었던 도구 4개를 tools/ 디렉토리 아래 파일별로 분리" — 발전 명시 |
| **CH06 사후 분류 / Router** | L146·176·687·784 | "Router는 ex06에서 하던 것처럼 질문 유형 라벨을 붙이는 용도로만 호출" — CH06과 동일 역할 유지 명시 |
| **CH05 사서 비유** | L103·123·461·576·588 | 7.1 팀장 첫 대사 "같은 질문에 매번 서가를 뒤지는 사서가 있으면 어떻겠어요?" — CH05 사서 4단계 비유에 "메모장·매뉴얼" 두 도구를 추가하는 발전 |
| **CH05 WindowMemory (대화용 메모장)** | L198 | "챕터 5에서 사서에게 대화용 메모장(WindowMemory)을 줬던 기억이 떠올랐습니다. 이번에는 메모장을 두 권 더 얹습니다" — 메모장 메타포의 명시적 확장 |
| **CH06 ex06 서버 핸드오프** | L732 | "챕터 6의 ex06 서버가 켜져 있다면 Ctrl+C로 먼저 종료합니다. ex07도 같은 8000 포트를 사용하기 때문에" — 운영 연속성 명시 |
| **CH04~CH05 LCEL 파이프라인** | L728·858 | "검색 도구(search_documents)가 반환한 문서는 AgentExecutor의 intermediate_steps에 고스란히 남아" + 그림 7-10 도구 04 `search_documents · LCEL · Top-k` |
| **그림 5-9 → 6-7 → 7-10 아키텍처 일관성** | L788~876 | `arch11` 컴포넌트 + `ops 3 chip new` 뱃지로 CH05~CH06 누적 위에 Cache·Monitor·Retry 3개 chip 추가 — 시각 회수 |
| **다음 챕터 예고** | L900 | ":::remember 챕터 8부터는 이 위에서 검색 품질 자체를 튜닝합니다" — CH08 검색 품질 튜닝으로 명시 위임 |

**핵심 회수 포인트(CH07 = "통합 에이전트 → 운영 안정화 흐름")**: 체크리스트 명시 의무 그대로 이행. CH06의 IntegratedAgent를 "그대로 두고 바깥을 감싸는" 구조 결정이 챕터 시작(L133)·중간(L582)·그림(L186·L629)·코드(L692)·FAQ(L784)에 5회 이상 일관 반복되어 챕터 간 메시지 일관성이 매우 높음.
