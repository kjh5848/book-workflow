# CH05 친절도 점검 보고서

> 11질문 패널 점검. CH05는 LCEL 체인 + WindowMemory 실습 챕터(실습 1~2 + 채팅 UI 검증). 강화된 Q4 기준(메타 내레이션·구어체 잔재·`~봅니다`·`~봤어요` 등) + Q11(CH01·CH02·CH03·CH04 회수) 동시 점검.
>
> **Q6/Q7 점검 범위**: CLAUDE.md 독자 상수 "배경지식 있음, 이 책의 주제만 모름" 적용. 일반 웹/인프라 용어(FastAPI·uvicorn·ASGI·Ollama 패키지·Python `deque`·`itemgetter` 등)는 정의·표 등재 면제. **도메인 용어**(LCEL·Retriever·Prompt·Parser·StrOutputParser·ChatPromptTemplate·WindowMemory·Source Grounding·그라운딩·환각·ConversationManager·세션 TTL·ReAct·Tool Calling 등)만 점검.

## 한눈에 보기

| # | 영역 | 판정 | 핵심 위반 |
|---|------|------|----------|
| 1 | 서사 연속성 (챕터 안) | ✅ PASS | — |
| 2 | 캐릭터 등장 일관성 | ❌ FAIL | 팀장 부재(0회) + 오픈이 대사 0회 + 5.3·5.4 본문 280줄 캐릭터 부재 |
| 3 | 비유 회수 | ✅ PASS | 사서·도서관 비유(5.1·5.2·5.3·5.4·5.5·5.6·remember까지 7회 회수). 메모장 비유(5.4)도 자체 완결 |
| 4 | 본문 톤 (종결어미·구어체·메타) | ❌ FAIL | **본문 구어체 `~봤어요` 1건**(line 101) + **`되죠/뜻이죠` 본문 종결 2건**(line 394·524) + **메타 내레이션 "그 사람"** 1건(line 388) |
| 5 | 캐릭터 대사 톤 | ✅ PASS | 동료 3회·방문자/사서 3회 모두 존댓말. 반말·`~고요` 0건 |
| 6 | 용어 첫 등장 처리 | ⚠ CONDITIONAL_PASS | **TTL**(line 36·359) 정의 부재. **WindowMemory**(line 8) `:::term-box` 없음(본문 풀이로 대체) |
| 7 | 용어 정리 표 완결성 | ❌ FAIL | **ConversationManager가 표에 있으나 본문 등장 0건**(line 533) — 본문에는 `session_manager.py` 파일명만 등장. 양방향 대조 위반 |
| 8 | 실습 코드블록 앞 안내 | ⚠ CONDITIONAL_PASS | 실습 1(`_format_docs`+`build_rag_chain` 두 블록) 첫 코드블록 앞에 별도 안내 없이 `[설명]` 블록 직후 한 단락(line 252)으로 통합 안내. 실습 2는 표준 충족 |
| 9 | 코드 후 설명 충분성 | ✅ PASS | LangChain 부품 3개 표(line 300~304) + `:::term-box`(line 306) "왜 chain·retriever 같이 반환?" + 5.5 채팅 UI 설명까지 풍부 |
| 10 | 섹션 전환 부드러움 | ⚠ CONDITIONAL_PASS | 5.2→5.3, 5.5 그림 5-7→5.6, 5.6 그림 5-9→마무리 단락 캐릭터 다리 부재. 5.1→5.2(사서 비유)·5.3→5.4(방문자 대사)는 모범 |
| 11 | 이전 챕터 맥락 회수 | ✅ PASS | CH01 그라운딩·도서관 사서·환각·RetrievalQA / CH02 사용자·FastAPI·PostgreSQL / CH04 벡터 검색·CLI·ChromaDB·임베딩 회수 ✅. CH03 메타데이터/문서 표준은 직접 회수 없음 (CH04에서 소화 완료, 자연스러움) |

**종합**: PASS 5 / CONDITIONAL_PASS 3 / FAIL 3 (총 11개 중)

## 우선 보강 항목

### FAIL (즉시 수정 필요)

**Q2. 캐릭터 등장 — 삼각 구도 붕괴 (FAIL)**

`storytelling.md` "캐릭터 삼각 구도" 룰: 한 챕터에 팀장·동료·오픈이 셋 모두 등장 + 부재 5~7페이지 초과 금지.

| 캐릭터 | 등장 라인 | 횟수 | 분포 |
|--------|---------|------|------|
| 동료 | 103·104·128·386 | 4 | 도입 3회 + 결말 1회 |
| 팀장 | — | **0** | **챕터 전체 미등장** |
| 오픈이 (대사) | — | **0** | **챕터 전체 미대사** |
| 오픈이 (내면독백) | 130 | 1 | 도입부 1회만 (`*맞다. 사람들은 문서 조각을…*`) |
| 방문자/사서 (비유 캐릭터) | 314·315·316 | 3 | 5.4 비유 도입 |

**3대 위반**:
1. **팀장 0회** — 5개 챕터 연속(CH01·CH02·CH03·CH04 모두 등장) 후 CH05에서 부재. 룰 "2개 챕터 연속 캐릭터 부재 금지"는 챕터 단위 적용이지만 **삼각 구도 부재 자체가 룰 위반**
2. **오픈이 대사 0회** — 내면독백 1회뿐. 동료의 질문(line 128 "이걸 제가 읽어요?")에 답하는 화자가 사라짐. 챕터 4의 line 786·790처럼 분석 톤 대사가 5.5에 들어가야 자연스러움
3. **5.3 LCEL 조립~5.4 WindowMemory** 본문 약 **280줄**(line 100~383) 동안 인물 부재. 비유 캐릭터(방문자/사서) 3대사가 line 314~316에 끼어 있지만 이는 **비유 속 인물**이라 실제 동료·팀장·오픈이 부재 보완 효과 없음

**수정 권장**:
- 5.3 도입(line 180~182) 또는 5.3 출처 강제 도입(line 226 직후) **팀장 한마디** 추가. 예: 팀장이 옆에서 *"제공 문서에서만이라고 못 박아 둬요. 안 그러면 또 끼워서 답해요"* 같은 소크라테스식 한마디로 그라운딩 강화 동기 촉발
- 5.5 채팅 UI 결과 직후(line 388) 동료 대사 옆에 **오픈이 대사** 1줄 추가. 예: *"이제는 검색 결과를 사용자가 직접 안 읽어도 되네요. 출처 라벨이 자동으로 붙으니 검증도 쉽고요."* — 도입 line 128 동료 질문에 대한 명시적 회수
- 5.6 그림 5-9 직후(line 524 끝) **오픈이 내면독백** *"파이프라인은 그대로, 도구로 감싸진다는 게 챕터 6의 그림이지."* 추가로 챕터 닫기 강화

---

**Q4. 본문 톤 — 금지 패턴 4건 검출 (FAIL)**

강화된 Q4 기준에서 4건 위반. CH04 FAIL(`~봅니다` 2건+`~어요/었어요` 3건)과 패턴 변형되어 살아남음.

#### 본문 구어체 잔재 (1건)
- **line 101**: "점심 먹고 돌아오니 그 동료가 또 의자를 돌려 제 쪽을 **봤어요**." — 본문 서술 (대사 외). `style.md` 종결어미 5종(`~합니다·~입니다·~세요·~보겠습니다·~습니다`) 외. CH04 line 658과 동일한 패턴 잔존 → "**봤습니다**."

#### 본문 `~죠` 종결 (2건)
- **line 394**: "에이전트가 질문을 보고 어떤 도구를 열지 스스로 판단하게 **되죠**." — 합니다체 본문 흐름 중 `~죠` 단발 등장. `style.md` "한 문단 안에서 톤을 섞지 않는다" 위반 → "**됩니다**."
- **line 524**: "LLM이 도구 없이 한 가지 일(문서 검색 답변)만 한다는 **뜻이죠**." — 같은 패턴. 5.6 마무리 단락에서 톤 흔들림 → "**뜻입니다**."

#### 메타 내레이션 (1건)
- **line 388**: "오프닝에 '이걸 내가 읽어?' 하던 바로 **그 사람**입니다." — `storytelling.md` "Show, Don't Tell" + 친절도 체크리스트 "어색한 문구·메타 내레이션" 위반. 거리감 있는 화자가 등장인물을 가리키는 메타 표현. CH04 FIX 4에서 "판을 뒤집었던"·"그 사람"이 정리됐는데 CH05에서 동일 패턴 재발 → "오프닝에서 '이걸 내가 읽어?' 하던 동료가 화면을 들여다봤습니다." 또는 line 388 자체 삭제 후 동료 대사로 직접 연결

#### 검출되지 않은 항목 (PASS 영역)
- `~봅시다`·`~봅니다`·`~고요`·`~구요` 0건 ✅
- AI 선호어(드디어·마침내·비로소·진정한·결국 이 모든 것) 0건 ✅. 단, **챕터 제목** "드디어 답해준다"의 "드디어"는 AI 선호어이지만 **파일명·외부 표제로 시각적 캐치**여서 본문 톤 위반은 아님 (참고: 친절도 검토는 본문 한정)
- 정의되지 않은 비유 도약 0건 ✅

**수정 패턴 요약**:
| 라인 | 현재 (FAIL) | 권장 (PASS) |
|------|------------|------------|
| 101 | 봤어요 | 봤습니다 |
| 388 | 그 사람입니다 | 동료가 화면을 들여다봤습니다 (또는 삭제) |
| 394 | 되죠 | 됩니다 |
| 524 | 뜻이죠 | 뜻입니다 |

---

**Q7. 용어 정리 표 완결성 — 표↔본문 비대칭 1건 (FAIL)**

용어 정리 표(line 528~534, 5건):

| 진짜 용어 | 본문 등장 | 상태 |
|----------|----------|------|
| **LCEL 파이프라인** | line 6·89·182·222·394·530 등 6회 이상 | ✅ |
| **출처 강제(Source Grounding)** | line 7·226·531 (3회) | ✅ |
| **WindowMemory** | line 8·90·310·331·332·359·379·532·541 (9회) | ✅ |
| **ConversationManager** | **표에만** 등장(line 533). 본문 0회 | ❌ **FAIL** |
| **파이프 연산자(\|)** | line 182·185·224·298·534 (5회) | ✅ |

**FAIL 사유**: `ConversationManager`가 표(line 533)에는 "메모장 관리인 → 세션별 WindowMemory를 보관하고 TTL로 만료 세션을 정리하는 클래스"로 정의됐으나 **본문 어디에도 클래스명으로 등장하지 않습니다**. 본문(line 359)에는 "`ex05/src/session_manager.py`가 세션별로 이 `WindowMemory`를 하나씩 보관하고 … TTL로 만료시킵니다"로 **파일명·동작 서술**만 있음. 친절도 체크리스트 "표에만 있고 본문에 안 쓰인 용어 → 본문 누락" 명시 위반.

**수정 권장 (둘 중 택일)**:
1. **본문 보강** (권장). line 359의 `session_manager.py` 설명을 "**ConversationManager**(메모장 관리인) 클래스가 `session_manager.py`에 정의돼 있어, 세션별로 이 `WindowMemory`를 하나씩 보관하고…"로 바꿔 클래스명을 본문에 노출
2. **표 정리**. ConversationManager 행을 표에서 제거하고 `세션 TTL` 같은 **이미 본문에 등장한** 용어로 교체

---

### CONDITIONAL_PASS (권장 보강)

**Q6. 용어 첫 등장 — 도메인 용어 정의 누락 2건 (CONDITIONAL_PASS)**

#### 한국어(English) + 정의 동반 (모범 9건)
- line 6·182 **LCEL(LangChain Expression Language)** — 영문 풀이 + 파이프 연산 풀이 ✅
- line 7·226 **출처 강제(Source Grounding)** — `:::term-box` 없지만 본문에서 5.3 절 전체로 풀이 ✅
- line 224·298·534 **파이프 연산자(`|`)** — 유닉스 셸 비유 + "같은 인터페이스만 지키면 자리 바꿔 낄 수 있습니다" 풀이 ✅
- line 228 **그라운딩** — CH01 회수 + "강한 버전" 위치 명시 ✅
- line 285·291 **`StrOutputParser`** — 표(line 304) "AIMessage 객체에서 `.content`만 꺼내는 파서" 풀이 ✅
- line 277·302 **`ChatPromptTemplate`** — 표 "system·human 메시지 조립" 풀이 ✅
- line 298 **`itemgetter("question")`** — "입력 dict에서 question 키만 꺼내는 호출가능 객체" 풀이 ✅
- line 320 **슬라이딩 윈도우** — "메모장…최근 5장만" 비유 + line 322 그림으로 시각화 ✅
- line 351 **코사인 유사도(Cosine Similarity)** (5.3 출처 강제 박스 안에서 회수)

#### 정의 누락 (보강 권장 2건)
- **line 36·359 "TTL"** — `session_manager.py`(line 36) 주석과 본문(line 359 "일정 시간 동안 호출이 없으면 TTL로 만료시킵니다")에 등장. 도메인 용어(세션 만료 정책)이지만 한국어(English) 표기·정의 부재. 권장: "**TTL(Time To Live, 유효 시간)**. 세션이 살아 있을 시간 한도. 일정 시간 동안 호출이 없으면 TTL로 만료시킵니다"
- **line 8·90·310 "WindowMemory" 첫 등장 시 정식 정의 부재** — `:::goal`(line 8) "**WindowMemory(k=5)** 로 최근 5턴 멀티턴 대화를 유지합니다"는 한 줄 풀이가 있지만, **정식 정의는 5.4 절(line 320)** 에서 비유로만 등장. 표(line 532)에는 정확히 "최근 N턴 대화만 유지하는 슬라이딩 윈도우. `deque(maxlen=k)` 기반"으로 정의됨. 권장: 5.4 절 도입(line 312 직전)에 `:::term-box` 추가하여 본문에서도 정식 정의 명시

#### 면제 (배경지식)
- `Ollama`·`deepseek-r1:8b`·`uvicorn`·`FastAPI`·`Jinja2`·`HTML`·`CSS`·`JS`: 일반 인프라 도구
- `python3.12`·`venv`·`pip install`·`source .venv/bin/activate`·`copy`/`cp`: Python 환경 명령
- `collections.deque`·`maxlen`: Python 표준 라이브러리

---

**Q8. 실습 코드블록 앞 안내 — 실습 1 통합 안내 패턴 (CONDITIONAL_PASS)**

`code.md` "[실습] 코드블록 앞 필수 안내" 충족 점검.

| 실습 | 라인 | 상태 |
|------|------|------|
| 실습 1 (`_format_docs`) | line 252 직후 254 | ⚠ 통합 안내 — line 252에서 "`_format_docs()`와 `build_rag_chain()` **두 함수**에 TODO가 있습니다. TODO의 `pass`를 지우고 아래 코드를 작성합니다." 한 번 안내 후 `[실습 1]` 블록 두 개 연속(line 254·268). 두 번째 블록(`build_rag_chain`) 앞에는 별도 안내 없음 (line 266 "이어서 본체 조립…" 연결구로 대체). **표준 형식**(파일 경로 + TODO `pass` 명문)은 첫 번째에서 두 함수 모두 커버하므로 **충족**. 단 두 번째 코드블록 앞에 짧은 재안내가 있으면 더 친절 |
| 실습 1 (`build_rag_chain`) | line 266→268 | ⚠ 통합 안내에 포함됨. 별도 명문 없음 |
| 실습 2 (`WindowMemory`) | line 329→331 | ✅ "`ex05/src/conversation.py`는 `deque(maxlen=k)` 위에 얇은 래퍼를 씌워 '이전 대화'를 프롬프트에 넣을 형태로 바꿉니다. `save_turn`, `get_history`, `clear` **세 메서드**에 TODO가 있습니다. TODO의 `pass`를 지우고 아래 코드를 작성합니다." 표준 3요소(파일 경로 + 메서드명 + `pass` 안내) 모두 충족 |

**경계 사례 판정**: 실습 1은 두 함수가 같은 파일(`rag_chain.py`)에 묶여 있고 한 번에 안내했으므로 룰 위반은 아님. CH04 실습 1-1·1-2와 같은 통합 패턴(`code.md` "이어서 같은 파일…" 패턴) 가능. 다만 `[실습 1]` 헤더가 두 번 연속 등장(line 254·268)할 때 두 번째 블록 직전에 "이어서 본체 조립" 같은 짧은 다리 문장 외에 **명시적 파일 안내가 한 번 더** 있으면 안전. 권장: line 266 "이어서 본체 조립." 다음에 "같은 `ex05/src/rag_chain.py`의 `build_rag_chain()` 함수 TODO를 채웁니다." 한 줄 추가.

---

**Q10. 섹션 전환 — 평면 점프 3건 보강 권장 (CONDITIONAL_PASS)**

H2/H3 전환 7회 중 4회 자연스럽고 3회 평면적.

#### 자연스러운 전환 (모범 4건)
- 5.1 → 5.2 (line 132 "그 '요리'를 해 주는 RAG Q&A 엔진을 만듭니다" → line 134 "사서가 일하는 네 단계" 비유 도입) ✅
- 5.2 → 5.3 (line 178 그림 5-2 캡션 "이번 챕터에서 3~4번을 추가합니다" → line 180 "5.3 LCEL 조립") — **콘텐츠 다리는 있으나 캐릭터 부재**. ⚠
- 5.3 (출처 강제 → 실습) (line 230 RAG_SYSTEM_PROMPT 도입 → line 232 "### 실습(rag_chain.py)" → line 234 코드) ✅
- 5.4 → 5.5 (line 359 "TTL로 만료시킵니다" → line 361 "5.5 서버 실행" → line 363 "두 파일의 TODO를 모두 채웠으면 서버를 띄워…") ✅
- 5.5 → 5.6 (line 394 "파이프라인 자체는 버려지지 않습니다." → line 396 그림 5-8 비교 박스) ✅

#### 평면 전환 (보강 권장 3건)
- **5.2 → 5.3** (line 178 그림 5-2 캡션 → line 180 "5.3 LCEL 조립과 출처 강제(rag_chain.py)"). 비유 4단계(사서)에서 코드 조립으로 직접 점프. 캐릭터 다리(예: 오픈이 *"네 단계 중 3~4번을 LCEL로 조립한다."*) 또는 짧은 전환 문장 1줄 권장: "사서의 4단계 흐름을 그대로 LCEL로 조립합니다."
- **5.3 → 5.4** (line 308 `:::term-box` "왜 chain·retriever 같이 반환?" → line 310 "5.4 WindowMemory"). 코드 풀이 → 새 주제로 점프. 다리 권장: "체인을 조립했지만 한 가지가 빠졌습니다. **이전 대화를 기억하는 일**입니다." (현재 line 312 "도서관 방문자가 사서에게 이어서 묻습니다."가 다리 역할 시도 중이지만 **앞 절 결과 받기**가 약함)
- **5.5 line 388 → line 390** (line 388 "그 사람입니다" 메타 내레이션 → line 390 "처음으로 사용자가 쓸 수 있는 형태의 비서가 생겼습니다"). 단락 점프. Q4 메타 내레이션 수정과 함께 처리 권장: 동료 대사 + 오픈이 분석 대사를 line 388 자리에 넣고 line 390으로 자연스럽게 흐르도록

#### 모범 사례 (참고)
- 5.4 도입 (line 312 "도서관 방문자가 사서에게 이어서 묻습니다." → 방문자·사서 대사 3턴 → line 318 "여기서 '거기'는 2층 인문학 서가입니다") — **비유 대화로 멀티턴 컨텍스트 문제를 직관적으로 보여 주는 모범**. CH05의 가장 친절한 다리

---

## 상세 (서브에이전트 보고서 요약)

### 질문 1. 서사 연속성 — ✅ PASS

도입(5.1) "그냥 답을 알려줘" 동료 질문 → 5.2 사서 4단계 비유 → 5.3 LCEL 조립(3·4단계 추가) → 5.4 WindowMemory(멀티턴) → 5.5 채팅 UI(오프닝 동료 질문 답변) → 5.6 시스템 아키텍처 자리 흐름 명확.

절 도입부 회수 패턴:
- line 101 "챕터 4에서 벡터 검색까지 돌렸고, 동료에게 CLI 사용법을 알려 줬습니다" — CH04 5.5 CLI 검색 회수
- line 145 "챕터 4가 2번까지였다면, 이번 챕터가 **3, 4번을 추가**합니다" — 5.2 비유 + CH04 위치 명시
- line 228 "챕터 1에서 맛본 **그라운딩**의 강한 버전입니다" — CH01 그라운딩 `:::term-box` 회수
- line 266 "챕터 1에서 RetrievalQA가 내부에서 알아서 해주던 검색·프롬프트·LLM 호출을, 이번에는 Retriever부터 하나씩 꺼내서 파이프로 직접 조립" — CH01 RetrievalQA → CH05 LCEL 발전
- line 374 "병가 쓸 때 증빙 서류 필요해요?라고 물어보면 사내 취업규칙을 근거로 답이 돌아오고, 답변 끝에 출처 파일명이 붙습니다" — 5.1 동료 도입 질문 직접 회수
- line 388 "오프닝에 '이걸 내가 읽어?' 하던…" — 5.1 line 128 동료 질문 회수 (단 메타 내레이션 톤은 Q4 위반)

오프닝 동료(line 103·104·128) → 클로징 동료(line 386) 원형 서사 완결. 5.1 미해결 톤("이걸 제가 읽어요? 그냥 답을 알려주세요") → 5.5 결과 검증("아, 이제 답을 주네요")으로 깔끔히 닫힘.

### 질문 2. 캐릭터 등장 — ❌ FAIL

| 캐릭터 | 등장 라인 | 횟수 | 분포 |
|--------|---------|------|------|
| 동료 | 103, 104, 128, 386 | 4 | 도입 3 + 결말 1 |
| 팀장 | — | **0** | **부재** |
| 오픈이 (대사) | — | **0** | **부재** |
| 오픈이 (내면독백) | 130 | 1 | 도입부만 |
| 방문자/사서 (비유) | 314, 315, 316 | 3 | 5.4 비유 |

**삼각 구도 위반**: 팀장·오픈이 대사 둘 다 부재 ❌
**원형 서사 충족**: 도입 동료 → 결말 동료 ✅
**부재 구간**: line 130 (오픈이 내면독백) ~ line 386 (동료 재등장), 약 **256줄/4페이지** 동안 캐릭터 대사 0건. line 314~316 비유 캐릭터(방문자·사서)가 5.4 도입에서 다리 역할 일부 수행.

CH04 친절도 보고서와 비교:
- CH04 Q2 CONDITIONAL: 555줄 부재. 단 동료 4회·팀장 1회·오픈이 대사 2회·내면독백 3회로 **삼각 구도는 유지**
- CH05 Q2 FAIL: **팀장·오픈이 대사 모두 0회로 삼각 구도 자체가 붕괴**. 부재 길이는 CH04보다 짧지만(256줄 vs 555줄) **인물 종류 부재가 더 심각**

### 질문 3. 비유 회수 — ✅ PASS

CH05의 핵심 비유 **사서·도서관**이 챕터 전체에서 7회 회수·발전:

| 위치 | 회수 형태 |
|------|----------|
| line 1 (챕터 제목) | "사서가 답해준다. LCEL 파이프라인" — 비유=H1 |
| line 97 (그림 5-1 캡션) | "사서가 답해준다. 책을 찾아서 읽고, 출처와 함께 답해 줍니다" — 비유 도입 시각화 |
| line 132 | "벡터 검색은 **재료를 찾아 주는 일**이지 **요리를 해 주는 일**이 아닙니다" — 사서 비유 + CH04 주방 비유 회수 |
| line 134~145 (5.2) | "사서가 일하는 네 단계" 비유 전체 도입. 듣기·서가에서 찾기·읽고 정리·출처 알려주기 1:1 매핑 |
| line 312~316 (5.4) | "도서관 방문자가 사서에게 이어서 묻습니다" — 비유로 멀티턴 컨텍스트 문제 풀이 |
| line 318·320 | "사람 사서는 당연히 기억하지만 LLM은 기본적으로 **기억력이 없습니다**" / "사서가 메모장을 들고 있다고 생각하시면 됩니다" — 비유 → 기술 연결 |
| line 520 (5.6) | "이번 챕터에서 사서(RAG Engine)와 통역사(LLM)가 자리잡으면서 **'질문 → 답변'의 한 줄 경로가 닫혔습니다**" — 비유 → 아키텍처 회수 |
| line 524 (5.6 마무리) | "사서가 장부실 열쇠까지 쥔 '하이브리드 사서'가 됩니다" — 비유 → 다음 챕터 6 예고 |
| line 539 (`:::remember`) | "벡터 검색은 재료, LCEL은 요리입니다" — 비유로 챕터 닫기 |

**보조 비유**:
- **메모장 비유**(line 320~325) — `deque(maxlen=5)`를 메모장으로 풀이. line 532 표·line 541 remember까지 회수
- **유닉스 셸 파이프**(line 224) — `ls | grep`으로 LCEL `|` 비유. 자체 완결

CH01 **사서 비유 + 도서관 서가** 회수:
- line 136 "지금까지 만든 벡터 검색은 **도서관의 검색 시스템**과 비슷합니다" — CH01 1.3 사서 비유 명시 회수
- line 138 "우리에게 필요한 건 **사서**입니다" — CH01의 사서를 CH05에서 **읽고 답하는 사서**로 발전

CH04 **주방 비유** 회수:
- line 132 "벡터 검색은 **재료를 찾아 주는 일**이지 **요리를 해 주는 일**이 아닙니다" — CH04 4단계(손질·다지기·양념·냉장고) → CH05 "요리"로 자연스럽게 연결. 비유 자산이 챕터 간 누적되며 발전

도입부에만 등장하고 단어로만 남은 비유 0건. **모든 비유가 회수되거나 발전**.

### 질문 4. 본문 톤 — ❌ FAIL

**금지 패턴 검출 4건** (강화된 Q4 기준 적용).

#### 본문 구어체 잔재 (1건)
- line 101: "그 동료가 또 의자를 돌려 제 쪽을 **봤어요**." — 도입부 본문 서술. CH04 line 658과 동일 패턴 잔존

#### 본문 `~죠` 종결 (2건) — 새 패턴
- line 394: "에이전트가 질문을 보고 어떤 도구를 열지 스스로 판단하게 **되죠**." — 5.5 마무리 단락
- line 524: "한 가지 일(문서 검색 답변)만 한다는 **뜻이죠**." — 5.6 ch-note 풀이

`storytelling.md` "대화체 톤 규칙"에서 `~죠`는 **대화·내면독백 안에서만 허용**. 본문 서술에서는 `~합니다·~입니다` 권장. CH04에서 검출되지 않은 새 패턴이 CH05에서 발생.

#### 메타 내레이션 (1건)
- line 388: "오프닝에 '이걸 내가 읽어?' 하던 바로 **그 사람**입니다." — 친절도 체크리스트 명시 위반("`~던 그 사람입니다`" 직접 예시). CH04 FIX에서 정리됐던 패턴이 CH05에서 재발

#### 검출되지 않은 항목 (PASS 영역)
- `~봅시다`·`~봅니다`·`~고요`·`~구요` 0건 ✅ — CH04 FIX 학습이 정착
- AI 선호어(드디어·마침내·비로소·진정한) 본문 0건 ✅ — 단 챕터 제목 "드디어"는 메타 표제이므로 본문 룰 적용 외
- `~인 거죠·~잖아요·~이거든요` 0건 ✅
- 정의되지 않은 비유 도약 0건 ✅

CH04와 비교:
- CH04 FAIL: `~봅니다` 2건 + `~어요/었어요` 3건 = **5건**
- CH05 FAIL: `~봤어요` 1건 + `~죠` 2건 + 메타 내레이션 1건 = **4건**. 패턴 변화하며 잔존. 룰 위반 명백

**수정 패턴 요약**:
| 라인 | 현재 (FAIL) | 권장 (PASS) |
|------|------------|------------|
| 101 | 봤어요 | 봤습니다 |
| 388 | 그 사람입니다 | 동료가 화면을 들여다봤습니다 (또는 삭제) |
| 394 | 되죠 | 됩니다 |
| 524 | 뜻이죠 | 뜻입니다 |

### 질문 5. 캐릭터 대사 톤 — ✅ PASS

| 캐릭터 | 대사 라인 | 톤 |
|--------|---------|-----|
| 동료 (line 103) | "병가 쓸 때 증빙 서류 필요해요?" | 존댓말 + 친근 |
| 동료 (line 104) | "연차 며칠 남았는지 어떻게 확인해요?" | 존댓말 |
| 동료 (line 128) | "이걸 제가 읽어요? 그냥 답을 알려주세요." | 존댓말 + 약간 짜증 톤 (Show, Don't Tell 모범) |
| 동료 (line 386) | "아, 이제 답을 주네요. 아까는 문서 조각만 던지더니." | 존댓말 + 안도 |
| 방문자 (line 314) | "한국 역사 관련 책 어디 있어요?" | 존댓말 (비유 캐릭터) |
| 사서 (line 315) | "2층 인문학 서가에 있습니다." | 존댓말 (비유 캐릭터) |
| 방문자 (line 316) | "거기 세계사도 있어요?" | 존댓말 |

반말·"야" 호칭·`~고요`·`~구요` 0건. 인용문 외부 반말 0건. 내면독백 (line 130 `*맞다. 사람들은 문서 조각을 원하는 게 아니라 답변을 원한다.*`)도 평서체 단문(`~다.`)으로 storytelling.md "내면독백" 룰 충족.

**다만 Q2 FAIL과 연동**: 팀장·오픈이 대사가 아예 없어서 **대사 톤 점검 대상 자체가 동료에 편중**. 톤 자체는 PASS이지만 **대사 다양성**이 부족.

### 질문 6. 용어 첫 등장 — ⚠ CONDITIONAL_PASS

도메인 용어 첫 등장 점검 (배경지식 면제 적용).

#### 한국어(English) + 정의·비유 동반 (모범 9건)
- line 6 **LCEL(LangChain Expression Language)** — `:::goal`에서 영문 풀이 + line 182 본문 정식 정의 + 그림 5-3 시각화
- line 7 **출처 강제(Source Grounding)** — 한국어(English) + line 226 5.3 절 전체 풀이 + 4개 규칙 표
- line 224 **파이프 연산자(`|`)** — 유닉스 셸 비유 + "같은 인터페이스" 핵심 풀이
- line 228 **그라운딩** — CH01 회수 + "강한 버전" 위치 명시
- line 285·291 **`StrOutputParser()`** — 표(line 304) "AIMessage 객체에서 `.content`만 꺼내는 파서" 풀이
- line 277·302 **`ChatPromptTemplate.from_messages([...])`** — 표 "system·human 메시지 조립" 풀이
- line 298 **`itemgetter("question")`** — "입력 dict에서 question 키만 꺼내는 호출가능 객체" 풀이
- line 320 **슬라이딩 윈도우** — "메모장…최근 5장만" 비유 + line 322~323 그림 시각화 + line 325 `collections.deque(maxlen=5)` 풀이
- line 390 **Tool Calling + ReAct 패턴** — "챕터 6에서…얹습니다" 위임. 본격 정의는 CH06이지만 한 줄 풀이("문서 질문과 DB 질문을 한 에이전트가 동시에 처리") 동반

#### 정의 누락 (보강 권장 2건)
- **line 36·359 "TTL"** — `session_manager.py`(line 36) 주석과 본문(line 359)에 등장. 도메인 용어(세션 만료 정책)이지만 한국어(English) 표기·정의 부재. 권장: "**TTL(Time To Live, 유효 시간)**. 세션이 살아 있을 시간 한도"
- **line 8 "WindowMemory(k=5)" 정식 정의 위치** — `:::goal`(line 8)에서 한 줄 풀이는 있지만, **5.4 절 도입(line 310~320)** 에서도 비유 위주로만 진행. 표(line 532)에는 정확히 정의됐지만 본문에 `:::term-box` 등 명시 정의가 없음. 권장: 5.4 절 도입에 `:::term-box` 추가하거나 line 318~320 "사람 사서는 당연히 기억하지만…" 직후에 한 문장 추가: "**WindowMemory**는 최근 N턴 대화만 유지하는 슬라이딩 윈도우 클래스입니다. `deque(maxlen=k)`를 래핑한 구조입니다."

#### 면제 (배경지식)
- `Ollama`·`deepseek-r1:8b`·`uvicorn`·`FastAPI`·`Jinja2`·`HTML`·`CSS`·`JS`: 일반 인프라
- `python3.12`·`venv`·`pip install`·`source`/`copy`: 환경 명령
- `collections.deque`·`maxlen`: Python 표준 라이브러리

대조: line 306 `:::term-box` "왜 chain·retriever 같이 반환?"는 친절한 풀이. **TTL·WindowMemory에도 같은 패턴 적용 권장**.

### 질문 7. 용어 정리 표 — ❌ FAIL

용어 정리 표(line 528~534, 5건):

| 본문 표현 | 진짜 용어 | 본문 등장 | 상태 |
|----------|----------|----------|------|
| "사서의 업무 순서" | **LCEL 파이프라인** | 6, 89, 182, 222, 394, 530 등 6+ | ✅ |
| "근거를 대" | **출처 강제(Source Grounding)** | 7, 226, 531 | ✅ |
| "5장짜리 메모장" | **WindowMemory** | 8, 90, 310, 331, 332, 359, 379, 532, 541 | ✅ (단 정식 정의는 표에만 — Q6 보강 항목) |
| "메모장 관리인" | **ConversationManager** | **0회** | ❌ **FAIL — 표에만 존재** |
| "입력 → 출력 연결" | **파이프 연산자(`\|`)** | 182, 185, 224, 298, 534 | ✅ |

**FAIL 사유 상세**: 표 4번째 행 `ConversationManager`(line 533)는 클래스명으로 정의됐으나 본문에는 `session_manager.py` 파일명·"세션별 Memory + TTL"(line 36 주석)·"세션별로 이 WindowMemory를 하나씩 보관하고…TTL로 만료시킵니다"(line 359) 형태로만 등장. **클래스명 `ConversationManager`가 본문 어디에도 없음**.

친절도 체크리스트 명시: "**표에만 있고 본문에 안 쓰인 용어 → 본문 누락**". 본문 누락은 **양방향 대조 위반**으로 FAIL.

#### 본문에 있고 표에 빠진 도메인 용어 (점검)
- `Retriever`·`Prompt`·`LLM`·`Parser`(line 6 LCEL 4부품) — 표에서 LCEL 파이프라인 행에 일괄 포함됨 ✅
- `StrOutputParser()` — 표에 별도 행 없으나 "Parser"의 구체화로 LCEL 행에 포함 인정 ✅
- `ChatPromptTemplate` — 표에 별도 행 없으나 "Prompt"의 구체화 ✅
- `그라운딩` — CH01 표에 등재됨 (CH05는 강한 버전 변형). 표 누락 아님 ✅
- `슬라이딩 윈도우` — WindowMemory 행에서 정의로 흡수 ✅
- `Source Grounding` — 출처 강제 행에서 영문 표기 인정 ✅

본문에 있고 표에 빠진 도메인 용어: 0건. **표에만 있고 본문에 없는 용어 1건(ConversationManager)** 만 FAIL 사유.

#### 수정 권장 (둘 중 택일)

1. **본문 보강** (권장): line 359의 `session_manager.py` 설명 부분을 다음과 같이 수정.
   ```
   `ex05/src/session_manager.py`의 **ConversationManager** 클래스가 세션별로 이 `WindowMemory`를 하나씩 보관하고, 일정 시간 동안 호출이 없으면 TTL(Time To Live, 유효 시간)로 만료시킵니다.
   ```
   클래스명 노출 + TTL 정의 동시 보강 (Q6 누락도 함께 해결)

2. **표 정리**: ConversationManager 행을 표에서 제거하고, 본문에 이미 등장한 다른 도메인 용어로 교체. 예: `세션 TTL`·`멀티턴 대화` 등

권장 안: **1안** — 표 유지 + 본문 보강이 클래스 구조 이해에도 더 친절

### 질문 8. 실습 코드블록 앞 안내 — ⚠ CONDITIONAL_PASS

`code.md` "[실습] 코드블록 앞 필수 안내" 점검.

| 실습 | 라인 | 안내 문장 | 평가 |
|------|------|----------|------|
| 실습 1 (`_format_docs`) | line 252 → 254 | "**`_format_docs()`와 `build_rag_chain()` 두 함수**에 TODO가 있습니다. TODO의 `pass`를 지우고 아래 코드를 작성합니다." | ⚠ 두 함수를 한 번에 안내. 첫 번째 코드블록(`_format_docs`) 직전이라 표준 충족 |
| 실습 1 (`build_rag_chain`) | line 266 → 268 | "이어서 본체 조립. 챕터 1에서 RetrievalQA가…Retriever부터 하나씩 꺼내서 파이프로 직접 조립합니다." | ⚠ 별도 파일 안내·TODO `pass` 명문 부재. 통합 안내(line 252)로 커버되지만 두 번째 블록 직전에 재안내 권장 |
| 실습 2 (`WindowMemory`) | line 329 → 331 | "`ex05/src/conversation.py`는 `deque(maxlen=k)` 위에 얇은 래퍼를 씌워 '이전 대화'를 프롬프트에 넣을 형태로 바꿉니다. **`save_turn`, `get_history`, `clear` 세 메서드**에 TODO가 있습니다. TODO의 `pass`를 지우고 아래 코드를 작성합니다." | ✅ 표준 3요소(파일 경로 + 메서드명 + `pass`) 모두 충족 |

**경계 사례 판정**: 실습 1은 같은 파일(`rag_chain.py`)의 두 함수를 묶어 한 번에 안내. CH04 실습 1-1·1-2(`main.py`의 `step1_python_parsing` + `main()`) 같은 통합 패턴이지만, CH04는 두 코드블록 직전에 모두 명시 안내(line 182·196)가 있는 반면 CH05는 두 번째 블록(`build_rag_chain`) 앞에 별도 명문 안내 없음.

**룰 위반 여부**: `code.md` "**`[실습]` 코드블록 바로 위에 반드시 다음 두 가지를 포함**"의 (1) 파일 경로 + (2) "TODO의 `pass`를 지우고 아래 코드를 작성합니다" 안내가 line 268 직전에는 부재. 단, line 252에서 두 함수를 한 번에 커버했으므로 **약한 위반(CONDITIONAL)**.

**수정 권장**: line 266 "이어서 본체 조립" 다음에 한 줄 추가:
```
같은 `ex05/src/rag_chain.py`의 `build_rag_chain()` 함수 TODO를 채웁니다.
```

이 한 줄로 `[실습 1]` 헤더 두 번 등장에 모두 명시 안내 충족.

`[실습 N]` 코드블록 헤더 형식도 일관: `[실습 1] ex05/src/rag_chain.py. _format_docs` (line 254)·`[실습 1] ex05/src/rag_chain.py. build_rag_chain` (line 268)·`[실습 2] ex05/src/conversation.py. WindowMemory` (line 331). 모두 `[실습 N] 파일경로. 제목` 표준 매칭.

`[설명]` 코드블록(line 234 RAG_SYSTEM_PROMPT 발췌)·`[터미널]` 코드블록(line 17·48·57·365)도 정상.

### 질문 9. 코드 후 설명 — ✅ PASS

| 실습 | 코드블록 직후 풀이 |
|------|-------------------|
| 실습 1 (`_format_docs`, line 254) | line 266 "각 문서에 `[출처: 파일명 (p.N)]` 라벨을 붙여 한 문자열로 합칩니다. 이 결과가 프롬프트 변수 `{context}`로 들어갑니다." 핵심 동작 1줄 + 다음 단계 연결 ✅ |
| 실습 1 (`build_rag_chain`, line 268) | line 298~308 매우 풍부 — (1) line 298 "파이프 연산자가 각 단계의 출력을 다음 단계의 입력으로 넘깁니다" 핵심 동작 1줄. (2) line 298 `itemgetter` 풀이. (3) line 300~304 **LangChain 부품 3개 표** (`ChatPromptTemplate`·`build_llm`·`StrOutputParser`). (4) line 306~308 `:::term-box` "왜 chain·retriever 같이 반환?" 설계 의도 명시. **CH04와 동등한 친절 수준** ✅ |
| 실습 2 (`WindowMemory`, line 331) | line 359 "`deque(maxlen=5)`가 자동으로 오래된 턴을 제거해 주므로 우리가 할 일은 'append → 텍스트 변환 → 비우기' 셋뿐입니다. `ex05/src/session_manager.py`가 세션별로 이 `WindowMemory`를 하나씩 보관하고, 일정 시간 동안 호출이 없으면 TTL로 만료시킵니다." 핵심 동작 + 외부 협력 클래스 역할 ✅ |

매개변수 3개 이상이면 표(line 300~304 LangChain 부품 3개)·`:::term-box`(line 306) 충족. 외부 주입 매개변수 출처 명시(line 285 `_format_docs`·line 272 `build_llm()`·line 274 `build_retriever()` — 각각 자기 함수 내 정의 또는 외부 모듈 import 명시). "왜 이 구조"가 line 224 (LCEL 부품화 이유)·line 230 (출처 강제 4규칙 이유)·line 306 (chain·retriever 함께 반환 이유)·line 633 (저장과 같은 임베딩 모델 이유, 단 5.5 인용 — CH04에서 회수)에서 명시. **Q9는 친절도 검토에서 가장 풍부한 영역**.

### 질문 10. 섹션 전환 — ⚠ CONDITIONAL_PASS

H2/H3 전환 7회 중 4회 자연스럽고 3회 평면적.

#### 자연스러운 전환 (모범 4건)
- 5.1 → 5.2 (line 132 "그 '요리'를 해 주는 RAG Q&A 엔진을 만듭니다" → line 134 "사서가 일하는 네 단계") — 내용 흐름 완벽 ✅
- 5.3 출처 강제 → 실습 (line 230 RAG_SYSTEM_PROMPT 도입 → line 232 "### 실습(rag_chain.py)" → line 234 코드) ✅
- 5.4 → 5.5 (line 359 TTL 만료 → line 361 "5.5 서버 실행 + 채팅 UI" → line 363 "두 파일의 TODO를 모두 채웠으면…") ✅
- 5.5 → 5.6 (line 394 "파이프라인 자체는 버려지지 않습니다." → line 396 그림 5-8 비교 박스 시작) — 내용 다리 ✅

#### 평면 전환 (보강 권장 3건)
- **5.2 → 5.3** (line 178 그림 5-2 캡션 → line 180 "5.3 LCEL 조립과 출처 강제(rag_chain.py)"). 비유 4단계에서 코드 조립으로 직접 점프. 캐릭터 다리 또는 다리 문장 1줄 권장
- **5.3 → 5.4** (line 308 `:::term-box` 닫힘 → line 310 "5.4 WindowMemory"). 코드 풀이 → 새 주제 점프. line 312 "도서관 방문자가 사서에게 이어서 묻습니다" 가 다리 시도이지만 **앞 절 결과 받기**가 약함
- **5.5 line 388 → line 390** (line 388 "그 사람입니다" → line 390 "처음으로 사용자가 쓸 수 있는 형태…"). 메타 내레이션 단락 점프. Q4 수정과 연동 처리 권장

#### 모범 사례 (참고)
- 5.4 도입 (line 312~316 방문자·사서 멀티턴 대화 3턴 → line 318 "여기서 '거기'는 2층 인문학 서가입니다") — **비유 대화로 컨텍스트 문제를 직관적으로 보여 주는 모범**. CH05의 가장 친절한 다리

CH04 친절도 보고서의 Q10 패턴(절 사이 다리 부재)이 CH05에서도 일부 재현. 다만 **CH05는 비유 대화(방문자·사서)로 다리를 잇는 5.4 도입이 모범**으로 분류 가능.

### 질문 11. 이전 챕터 맥락 회수 — ✅ PASS

CH01·CH02·CH03·CH04 회수 점검 (4개 챕터 동시 적용).

#### CH01 회수 (체크리스트 4종)
1. **사서·도서관 비유** — line 134~145 (5.2 사서 4단계 도입) + line 312~316 (방문자·사서 멀티턴 비유)에서 직접 회수·발전. CH01의 "사서가 책을 찾아 건네기"가 CH05에서 "사서가 읽고 답변까지 정리"로 발전 ✅
2. **그라운딩** — line 228 "챕터 1에서 맛본 **그라운딩**의 강한 버전입니다. 그때는 '참고 정보에서만 답하세요' 한 줄이었지만…" CH01 line 494 `:::term-box` 그라운딩 풀이의 직접 회수 ✅
3. **RetrievalQA** — line 266 "챕터 1에서 RetrievalQA가 내부에서 알아서 해주던 검색·프롬프트·LLM 호출을, 이번에는 Retriever부터 하나씩 꺼내서 파이프로 직접 조립합니다" CH01 line 479~484 회수 + LCEL 발전형 명시 ✅
4. **환각** — line 7 `:::goal`·line 252 "규칙 2번이 **환각 방지**" — CH01 1.1 환각 회수 ✅
5. **그림 1-12 (도착점 시스템 아키텍처)** — line 455~518 **5.6 arch11 룩북**이 그림 1-12의 발전형. NEW 뱃지(line 471 "Runtime · RAG 체인" + line 489 "외부 · LLM (External) Ollama")로 신규 자리 표시 ✅

#### CH02 회수 (체크리스트 3종)
1. **커넥트HR 에이전트** — line 9 `:::goal`·line 235 "당신은 커넥트HR 사내 문서 Q&A 비서입니다"·line 372 "커넥트HR 에이전트의 전신. RAG Q&A 채팅 UI"·line 456 "사서가 답해준다 - RAG Engine + LLM NEW" 5+회 명시 회수 ✅
2. **사용자·FastAPI·PostgreSQL** — line 467 arch11 박스에 명시 + line 511 "관계형 DB (Relational) PostgreSQL 직원·연차·매출" CH02 box 그대로 유지 ✅
3. **그림 번호 일관성** — CH02 그림 2-6의 arch11 다이어그램이 CH05 그림 5-9 룩북으로 발전. PostgreSQL 박스(line 511)는 CH02 그림에서 정의된 그대로 ✅

#### CH03 회수 (체크리스트 — 직접 회수 없음, 이유 명확)
- CH03(문서 표준·메타데이터·재인덱싱) 관련 직접 명시 회수는 0건. 그러나 **CH04에서 CH03 4규칙(파일 포맷·청킹·메타데이터·재인덱싱)을 모두 회수하며 코드로 구현 완료**한 상태이므로 CH05에서 다시 언급할 필요 없음. CH05의 출처 강제 프롬프트(line 235~250)에서 "출처: 파일명" 라벨이 CH03·CH04 메타데이터(`file_name`)와 자연스럽게 연결됨 — **간접 회수**

#### CH04 회수 (체크리스트 6종 — 가장 직결)
1. **벡터 검색·CLI** — line 101 "챕터 4에서 벡터 검색까지 돌렸고, 동료에게 CLI 사용법을 알려 줬습니다" CH04 5.5 cli_search 회수 ✅
2. **벡터 검색 결과(유사도 87.2%·81.5%)** — line 108~126 sr-result 박스가 CH04 line 641~650 dual-image 검색 결과의 톤·형식과 일관 ✅
3. **ChromaDB·임베딩 모델** — line 30·73 chroma_db 경로·line 72 `jhgan/ko-sroberta-multitask` 동일 모델 명시 + line 274 `build_retriever()`가 CH04 ChromaDB를 그대로 활용 ✅
4. **주방 비유 → 사서 비유 전환** — line 132 "벡터 검색은 **재료를 찾아 주는 일**" CH04 주방 비유 직접 호출 + 사서 비유로 자연 전환 ✅
5. **챕터 4까지 = 사서 4단계 중 1·2** — line 145 "챕터 4가 2번까지였다면, 이번 챕터가 **3, 4번을 추가**합니다" + line 161~177 그림 5-2 lf-done(챕터 4까지) vs lf-new(이번 챕터) 시각 구분 ✅
6. **CH04 미해결 문제(검색 71% 도착선·k=5 노이즈)** — CH04 line 658·660의 미해결("아직 완벽하지는 않다") → CH05에서는 **검색 품질 자체를 다루지 않고**(CH08 위임) **검색 결과를 LLM에 붙여 답변**으로 우회. CH04 line 790 오픈이 대사 "챕터 5에서는 이 검색 결과를 LLM에 붙여서 자연어 답변부터 받아 봐요"의 약속 정확히 이행 ✅

CH02 친절도 보고서의 Q11 학습("그림 번호 명시 인용 부재")·CH04 Q11 학습(arch11 룩북 NEW footer)이 CH05에서도 보강됨. **CH05의 그림 5-9는 CH02 그림 2-6 + CH04 그림 4-9의 자연 발전형**.

CH01 line 706·CH02 line 297·CH03 line 384·CH04 line 700 등 **4개 챕터의 각 마무리 절 약속**이 CH05에서 모두 이행. CH05는 친절도 검토에서 **이전 챕터 회수가 가장 일관된 챕터**.

---

## 다음 단계

CH05 친절도 종합 판정: **PASS 5 / CONDITIONAL_PASS 3 / FAIL 3**.

### 즉시 수정 (FAIL 3건)

#### 1. Q4 본문 톤 수정 (4개 라인)
| 라인 | 현재 | 권장 |
|------|------|------|
| 101 | 봤어요 | 봤습니다 |
| 388 | 그 사람입니다 | 동료가 화면을 들여다봤습니다 (또는 line 388 단락 자체를 동료/오픈이 대사로 교체) |
| 394 | 되죠 | 됩니다 |
| 524 | 뜻이죠 | 뜻입니다 |

#### 2. Q2 캐릭터 삼각 구도 보강
- 5.3 도입(line 180 또는 226) **팀장 한마디** 추가 — 그라운딩 강화 동기 촉발
- 5.5 채팅 UI 결과 직후(line 388 단락 교체) **오픈이 대사** 추가 — 동료 line 128 질문에 대한 명시 회수
- 5.6 그림 5-9 직후(line 524) **오픈이 내면독백** 추가 — 챕터 6 위임 자연스럽게

#### 3. Q7 ConversationManager 본문 등장 보강 (권장: 1안)
line 359 수정:
```
`ex05/src/session_manager.py`의 **ConversationManager** 클래스가 세션별로 이 `WindowMemory`를 하나씩 보관하고, 일정 시간 동안 호출이 없으면 **TTL(Time To Live, 유효 시간)**로 만료시킵니다.
```
→ Q6의 TTL 누락 + Q7의 ConversationManager 누락 동시 해결

### 권장 보강 (CONDITIONAL_PASS 3건)
1. **Q6 TTL·WindowMemory 정식 정의** — line 359 또는 line 312 직전에 `:::term-box` 추가 (Q7 보강과 연동)
2. **Q8 실습 1 두 번째 코드블록(`build_rag_chain`) 앞 안내** — line 266에 "같은 `ex05/src/rag_chain.py`의 `build_rag_chain()` 함수 TODO를 채웁니다." 한 줄 추가
3. **Q10 평면 전환 3건** — 5.2→5.3, 5.3→5.4, 5.5 line 388→390에 캐릭터 다리 또는 전환 문장 (Q2 보강과 연동)

### 친절도 검토 누적 패턴 (CH01~CH05)

CH04 FIX 1~5 학습이 CH05에서 일부만 정착:
- ✅ `~봅시다·~봅니다` 0건 (CH04 학습 정착)
- ✅ `~고요·~구요` 0건 (CH02 이후 누적 0)
- ❌ `~어요/었어요` 1건 잔존 (CH04 3건 → CH05 1건. 감소 추세이지만 0 아님)
- ❌ `~죠` 본문 종결 2건 — **새 패턴 발생** (CH04에는 없던 패턴)
- ❌ 메타 내레이션 1건 재발 — CH04 FIX 4 이후 처음 재발
- ❌ 캐릭터 삼각 구도 붕괴 — CH02·CH03·CH04에서 점진 회복 후 CH05에서 다시 팀장·오픈이 대사 부재

CH05는 **비유 회수(Q3 ✅)·이전 챕터 회수(Q11 ✅)·코드 후 설명(Q9 ✅)** 이 친절도 검토의 모범 영역이지만, **Q4 톤 새 패턴(`~죠`) + Q2 삼각 구도 붕괴 + Q7 표↔본문 비대칭**이 FAIL 3건으로 즉시 수정이 필요하다.

다음 챕터: `친절도 점검 CH06` (CH01~CH05 회수 포함, 11질문 모두).
